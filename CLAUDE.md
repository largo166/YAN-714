# CLAUDE.md — ROM-AI 工程约定

本文件是每次会话加载的工程铁律索引。详尽背景见 `MEMORY.md` 指向的记忆文件。

## 验证成本红线（本条最高优先，2026-07-06 立）

**当"验证某改动是否正确"的预估成本 > "做出该改动本身"的成本 2 倍时，必须立即停下、换验证策略、并向用户上报——不得继续消耗。**
> 方案改十分钟的问题，不允许用三小时的汇报去证明它改对了。

**验证方式选型（按改动类型选最低成本的够用手段）：**
| 改动类型 | 验证手段 |
|---|---|
| 状态/交互逻辑（向导回退、reducer、hook 状态机） | **组件级测试**（vitest + @testing-library/react，mock 外部依赖注入状态）——留仓库=永久回归守卫 |
| 接口契约（请求体/响应/状态码） | **API 断言**（Python urllib/httpx 直连精确字节；勿用 shell curl 传中文 body） |
| 视觉观感（空态文案/配色/布局/pulse） | **截图人审**（`?go=N` 跳板直达；证据类截图须来自 dist/打包形态，见下「构建形态使用规范」） |
| 端到端点击（CDP/Playwright 真浏览器） | **只留给真正的集成路径**，不用来验单点状态逻辑（成本最高，ROI 最低） |

配套：preview 工具 与 CDP-debug Chrome(9222) 是**两个独立浏览器实例**，别混用；dev(5173) HMR 载源码改动，exe/8000 托管的是 build 出的 dist。

## 构建形态使用规范（2026-07-07 立；禁"用途"，不禁"工具"）

dev（5173）不禁用——但它的产出**不构成结论**。证据必须来自更高形态。

1. **dev（5173）仅用于开发过程中的自查与调试**，其页面表现不构成任何结论依据。
2. **凡属证据类产出**——验收截图、视觉 diff、行为验证、性能观感、汇报材料——**必须来自 `npm run build` 后的 dist（标注 build 哈希）或打包形态**。
3. **涉及路径、数据源、origin、文件系统的问题**（即"入库显示不出"一族），**禁止在 dev 下下结论，必须在 pywebview 打包形态复现与验证**。
4. **任何汇报中的证据必须注明形态标签（dev / dist / packaged）**；无标签视为无效证据。
5. **agent 在声明"已修复/已验证"之前，最后一次验证必须发生在 dist 或更高形态。**

> **本规范自立项日（2026-07-07）起生效，不溯及既往证据；历史证据是否补做，由人决定，agent 不得自行发起补证。**（为历史证据补作业本身违反「验证成本红线」。）

## 数据操作铁律（2026-07-07 立；违反即停）

**第 0 条(排在"脚本先过我"之前):任何数据操作(删/改/迁移/去重)前,先验证目标库是「真实在用的库」+「物理文件完整」。**
- 验证:目标库的记录抽样能定位到真实物理文件(storage_root+stored_path 存在);storage_root 是当前机器真实路径,不是历史/别机残留。
- 不通过 → **停,不做**。在"元数据还在、物理文件早丢"的死库上清洗 = 清一堆指向不存在文件的记录,看着努力实则无价值(踩红线)。
- 通过后才轮到:脚本 dry-run 先过用户 → 备份 → 软删可回溯 → 记日志。
> 反面教材:差点在开发库(870 条物理文件全脱节的死库)上软删 530 条。见 [docs/postmortems/对着死库做手术.md]。

## 出包前固定检查（2026-07-07 立；违反即停，不出包）

病根:"悬置改动"（工作区里该进包却没提交的修复）长期存在,导致修复从未发布、bug 反复"复现"（实为从未上线）。**以后这个状态活不过一次打包。**

出包前三项硬性检查,缺一不打包:
1. **`git status` 必须干净**——工作区/暂存区零未提交改动。
2. **包从当前 commit 的干净 checkout 构建**——验证=提交=发出三者同一份代码（承接「构建形态使用规范」）。
3. **若工作区有任何未归属改动,先定归属再打包**——禁止带着悬置改动出包。归属未定的改动要么提交、要么 stash 出工作区,不允许"就那么躺着"跟着打包。

> 反面教材:DropMenu Portal 修复在工作区躺了多轮未提交,主线/exe 一直是旧版无 Portal → 下拉被卡片盖住"复现"。真相是修复从未发布。

## 裁决落档铁律（2026-07-07 立；防记忆漂移）

**用户每轮批复/裁决,当场追加到对应方案文档的「已批决议」一节,带日期。后续执行只认文档,不认对话记忆。**
- 触发:用户说"批准/定了/就这么办/改成X"等任何裁决 → 立即写进 `docs/decisions/` 或对应方案文档的「已批决议」段。
- **区分「待批」与「已批」是硬纪律**:方案里的建议默认是"待批",只有用户明确批复才转"已批"并落档。**严禁把"我建议保留"记成"用户已批保留"。**
- 执行前先读文档的「已批决议」,不凭记忆复述裁决。
> 反面教材(已发生 2 次):① hunk 拆分方案被用户否决后,多轮later 又"复活";② 存量清洗把用户的"待拉清单批归属"记成"已批保留"。记忆漂移比激进更隐蔽——方向看似保守,实则丢了裁决细节。

## 认知资产可移植性（Cognition Writeback，2026-07-07 立）
模型是租来的,认知资产才是自己的。凡未进 git 仓库的认知(规则/决议/复盘)= 换机器即丢 = 视为不存在。
- 认知容器:`CLAUDE.md`(铁律总纲) + `MEMORY.md`(索引) + `.claude/memory/`(回写文件) + `docs/decisions/`(已批决议,带日期) + `docs/postmortems/`(事故复盘)。
- 工作流 SOP 进 `.claude/skills/`(诊断先行/出包铁律/影响点分析/裁决落档)。
- 冷迁移演习:clone 仓库 + 拷 data 目录 → 全新环境 AI 应遵守出包铁律、认得已批决议。演习过才算"能力随安装走"闭环。
- **这也是 ROM-AI 的产品命题**:认知回写+上下文供给做成产品功能,让任何接入的模型都拿到项目认知。见 [docs/decisions/ROM-AI-认知资产可移植性.md]。

## 命名与元数据（2026-07-07；名字即元数据）
- **原始文件名永不丢弃**:入库时保留原始文件名(存字段),规范名是「附加」不是「替换」。文件名里常藏甲方发件语境(如"最终版-甲方确认"),这是认知资产,删了就没了。
- 三层命名规范(项目名 `城市-甲方-地块` / 文件入库名 `短码_阶段_类型_语义_版本` / 归属识别 词典→LLM→人工确认)见 [docs/decisions/三层命名规范.md]。stored_path 拍平后文件名是唯一归属线索,故命名=数据可信度第一道防线。

## 事件命名约定（数据同步，2026-07-07）
一类数据变更配一个专属事件,由该类数据的**所有**消费者监听——禁止"万能刷新事件"把所有 hook 拉一遍(那是数据源糊化)。已有:
- `romai:knowledge-updated`——知识库变更(入库完成)。消费者:数据基地首页大字/最近入库。
- `romai:settings-updated`——设置路径变更(仓库/工作目录/收件箱)。消费者:CleanupWizard(仓库)/CleanupOverlay(工作目录)/useKnowledgeLive(收件箱 pill)。
> 加新的路径/数据消费者时,必须一并订阅对应事件,否则就是下一个"设置↔X 不同步"。

## 其它铁律（详见 MEMORY.md）
- **不假按钮/改动克制而不过度/先确认再动手**；改动范围以"把问题真正解决"为准（用户已明确:没有"为少而少"的最小改动要求），但不顺手重构无关代码。任何 NL 路由只准"预填面板+一键确认，永不静默执行"。
- **三态诚实**（加载/失败/空态）是新板 DoD 硬性项，非打磨项——空态统一句式「暂无X。下一步指引。」
- **锁版视觉面**（开机影片/口令闸/五板构图/胶囊导航/1280×720 舞台度量+字号 token）冻结，改动须单独拍板，且与晕动症红线绑定。
