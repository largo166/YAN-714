# 今晚复盘(2026-07-09 夜)

> 一晚四大件全部落地 + 打包实测。此文档为认知回写(换机器可复现今晚决策链)。

## 一、完成清单(全部 commit + push backup + 打包/dist 实测)

| # | 事项 | 状态 | 关键证据 |
|---|---|---|---|
| 1 | **P1-1 收口 A1/A2/A3** | ✅ | 回流/沉淀补语义轴 + 过滤双轴 + 分布条切新轴;后端 237→251 passed;commit 724f209 |
| 2 | **OCR 进包复验** | ✅(packaged) | 扫描件 PDF→OCR 出字→检索命中(前轮已封,本轮 A1-3 顺带复验) |
| 3 | **开机影片换版 v12 海面地形** | ✅(packaged 四断言) | three.js 粒子海+每次开机都播+右下角 Skip;无声/skip 随时跳/播完进主界面全过;commit deb43e3 |
| 4 | **会议转写全链** | ✅(packaged ALL_PASS) | faster-whisper+sherpa-onnx+热词;冻结态真录音→转写(10 热词命中)→分离→改名→五段纪要;commit 78bc857 |

## 二、四大件各自的关键决策/坑

### 开机影片(v12)
- three.js 净新增依赖(0.185),粒子海独立 FilmSea 组件,局部 uniforms 与共享 SeaCanvas 隔离。
- **对抗审查抓 4 真缺陷 3 修 1 落档**:①0.185 色彩管理改调性→锁 LinearSRGB 复现 r128;②双 scrim 压暗→影片根 isolate z-2;③影片期 SeaCanvas 全遮挡仍满帧→phase!=='film' 才挂;④时间线外 tween 卸载清理。超宽屏构图边界落档不修(16:9 基准)。
- 冻结态坑:WebView2 窗口最小化/遮挡冻结 rAF(影片自动暂停),测试须 user32 带前台;这是正常 UX。

### 会议转写(P0)
- **缝早预留好**:Meeting.segments_json/transcript_source='asr' 是设计位从未通电,build_prompt 已吃 speaker_key——只补"录音→文字"一环,零新表零迁移。
- 热词=认知脊椎 gather_cognition(单一注入点)+ 默认建筑术语表 → initial_prompt,冻结态 10/13 命中。
- 分离用 sherpa-onnx(免 torch 复用 onnxruntime),dev 双音色实测分 2 说话人;SAPI 单音色只出 1 是夹具限制,align 纯函数单测锁。
- **诚实开口**:hf-mirror.com 对 faster-whisper repo 是 308 回源 HF(非自持),CN 无 HF 访问的机器下不动模型——本机能下因可直连 HF。已落档待负责人定 ModelScope 分支(开工令未含,不自扩张)。

## 三、归因与认知资产

- **"缝预留"的价值**:开机影片和会议转写都受益于此前架构留的口子(seaUniforms 解耦、segments_json 形状)——今晚的快,是过去克制的复利。
- **打包形态是唯一结论**:两件都在 exe 冻结态实测,不在 dev 下结论(构建形态规范)。会议转写还专门验了原生 DLL 从包内加载(collect_dynamic_libs)。
- **对抗审查兑现**:开机影片 diff 过 4 维审查+对抗验证,抓出色彩管理这种"看着对实则整片偏色"的隐蔽缺陷——ultracode 的价值点。
- **诚实红线守住**:mirror 回源开口没藏,如实上报并标"待负责人定",不自行扩张开工令外的范围。

## 四、路权与挂起

- 转写(③)已完成,路权按开工令走完。
- **资产池小样/实施**:前轮已出图2小样 + 接入点分析(数据基地入口带第三枚按钮,浮层同 CleanupWizard,唯一后端增量=改类型 PATCH)。等负责人批。
- **五板块粒子背景**:本轮出小样(见 docs/decisions/五板块背景小样-20260709.md),参考开机精致化、非波浪海。等负责人批。
- P1-2 统一命名/大纲提取等:维持排队。
