# 接力单 · 下次开机起点(2026-07-09 夜封存)

> 本轮全部提交已推 backup,本地 HEAD=backup=`9254444`,工作区干净。下次开机看这份,不翻对话记忆。

## 本轮做完了什么(都已 commit)
- **三个 bug 全修**:
  - bug1 系统目录拦截 + bug2 图片进资产库 → 根治(ingest A语义 + 跨项目资产视图),`15fbfb7`,**冻结态 exe 实测 FROZEN_PASS**。
  - bug3 会议纪要 → 解耦(无 key 也能看/导出转写稿 + 设置 key 提示明确),`9254444`,dev 测试过。
- 其它已落:开机影片 v12 换版、五板块粒子背景全线统一(FilmSea)、会议转写全链(ModelScope主路)、图片资产池、认知包、共创线约束压成一份。
- 测试:后端 259 passed、前端 tsc-b/build/vitest 22 全绿。

## 交付物
- **含 bug1+2 修复的 exe**:`release-internal/ROM-AI-bug1+2修复-20260709.exe`(keyless,gitignore 内不入库)。
  - ⚠️ **此 exe 不含 bug3**(bug3 在其之后提交)。要在 exe 验 bug3,需重打含 `9254444` 的包。

## 等负责人的事(下次开机第一时间确认)
1. **三条 bug1+2 验收结果**(负责人自己跑,有问题只发现象截图):①选市庄-00直接入库 ②选Pictures父级放行+多项目 ③跨项目资产池可见。用的是 bug1+2 那个 exe。
2. **要不要打含 bug3 的最终包**(全含三 bug 修复)——等三条验收报完再决定,还是现在打。
3. **粒子背景眼验**(全线统一后):开机影片→口令闸→五板块背景是否顺、够精致;验收三标准(30秒余光不吸走/存在感够但不抢/无卡顿)。任一不过只报现象,agent 不自行调参。
4. **下一阶段方向判断**(agent 已给):重心=打通"判断→汇报产出"执行层出口(共创卡容器+PPT骨架),网页采图排其后作支线。做不做/先做哪个,负责人裁。

## 硬约束(不变)
- 共创线唯一约束见 `docs/decisions/共创线-唯一约束.md`:"AI 自动建筑判断进产品线"封着(双门 N+盲评未过),其余解封。
- AGENTS.md 两条新铁律:说"确认过"前必须真查;同一验证失败2次即停转替代。
- key 是负责人的事,agent 不过问不提醒。

## 未完成/挂起(非本轮)
- 会议转写 hf-mirror 回源开口(已落 ModelScope 主路兜底,CN 可用)。
- 任务 #34 A语义前端向导确认屏(执行侧已补,前端确认屏未做)。
- 死代码待清:transcription.py 云 ASR provider 分支。
