# 检查点0/① 实施清单（据代码事实起草 · 待审）

> 起草依据：本会话一手盘点 config.py / database.py / main.py / uploads.py / models.py / parsing.py / retrieval.py / image_assets.py / project_files.py / alembic(env+0001~0022) / romai.spec / test_repository_root.py。
> 不引用任何别处会话结论。每条标 **NEW/改** + 文件 + 增删行量级 + 依赖。
> **本清单不含检查点① 实现代码**，只描述工作面供审。

---

## 盘点关键结论（决定 NEW vs 改的地基）

1. **启动流程不跑 alembic**：`main.py:31` → `init_db()` → 只 `create_all()` + deprecated 裸 ALTER（`database.py:76-118`）。空库能建表，**已有库列升级从不发生**。这是检查点0 的核心缺口。
2. **alembic 基建齐全**：`env.py:29` 已从 `settings.resolved_database_url` 取 URL、`render_as_batch=True`、链 0001→0022 连续、head=`0022_admin_passcode`、加列惯例=`batch_alter_table+add_column+server_default` 双向。→ 检查点0 是**接线**，不是从零建。
3. **exe 打包隐患**：`romai.spec:54` alembic 包在 hiddenimports，但 `backend/alembic/` 目录**未进 datas**。冻结态找不到 versions/*.py，`upgrade` 会失败。必须补。
4. **检查点① 四条管线全部已存在可复用**：解析(`parsing.py`,MAX_TEXT_CHARS=200000,truncated_at_page/total_pages,状态机)、切块(`content_chunks_json`)、FTS5 索引(`retrieval.py` knowledge_documents_fts)、抽图(`image_assets.py`+`_extract_and_store_assets`,80图上限)。→ N5"真切块"= 串起已有 parse→chunk→index，非重写解析。
5. **D2 helper 已存在**：`_managed_layout(db,project)`(project_files.py:30) 就是唯一的仓库根读取收敛点（`repo_root_for` 的现身）；`_migrate_project_to_repo`(:51) 已实现"文件搬进仓库"移动语义——B→A"文件层零改动"已被现有代码证实。
6. **三处真 NEW**：SSE、后台任务队列/DB-backed worker、content_hash（backend 零先例）；staging 端点（批量导入 2026-07 已删，git 可找回）。

---

# 检查点 0 —— 数据库地基（先做，先验收）

**目标**：已有数据的环境安装/启动 → 不崩、数据完好、备份文件在、schema 升到 head。

## A. 改造面（改现有文件）

| # | 动作 | 文件 | 量级 | 依赖 | 标 |
|---|---|---|---|---|---|
| 0-A1 | `init_db()` 里在 `create_all()` **之前**插入 `_run_migrations()`：嵌入式 `alembic command.upgrade(cfg,"head")`，cfg 用 `Config(alembic.ini 绝对路径)` + `set_main_option("sqlalchemy.url", settings.resolved_database_url)`。失败**中止启动并明确报错**（不静默继续、不留半升级库）。保留 create_all 仅兜空库自举。 | `backend/app/database.py` | ~+30 改 | alembic 已装、env.py 已就绪 | 改 |
| 0-A2 | `alembic.ini` 定位：新增 helper 解析 ini 路径（开发态 `BASE_DIR/alembic.ini`；冻结态 `_MEIPASS/alembic.ini`）。冻结态 `script_location` 需指向打包后目录。 | `backend/app/config.py`(+helper) 或 database.py | ~+12 改 | 依赖 0-B3 打包 | 改 |
| 0-A3 | exe 打包补 datas：`(str(BACKEND/"alembic"), "alembic")` + `(str(BACKEND/"alembic.ini"), ".")`，确保 versions/*.py 与 ini 进 `_MEIPASS`。 | `desktop/romai.spec` | ~+2 改 | — | 改 |

## B. 新建面（NEW）

| # | 动作 | 文件 | 量级 | 依赖 | 标 |
|---|---|---|---|---|---|
| 0-B1 | **REPOS_ROOT 常量**：`REPOS_ROOT = DATA_DIR / 'repos'`；模块导入时 `mkdir(parents=True, exist_ok=True)`；不可建/不可写 → 明确抛错（fail closed，复用 `validate_repository_root` 的可写探针范式）。**不另立可覆盖根**（D1）。 | `backend/app/config.py` | ~+10 NEW | — | NEW |
| 0-B2 | **启动前 DB 备份**：`_run_migrations()` 前，若 sqlite 且库文件存在 → 复制到 `DATA_DIR/backups/rom_ai.YYYYMMDD-HHMMSS.db`（WAL 模式需 `PRAGMA wal_checkpoint(TRUNCATE)` 后再拷，或连库拷主文件+`-wal`+`-shm`）。保留最近 N 份（如 5），旧的删。备份失败 → 中止升级（不拿没备份的库赌迁移）。 | `backend/app/database.py`(+helper) | ~+35 NEW | 在 0-A1 之前执行 | NEW |

## C. 边界（不动 / 红线）

- 不改任何**现有迁移文件** 0001~0022（历史已 stamp，改了会撕裂链）。
- 不删 `_ensure_columns()`（deprecated 但兜历史库，保留）。
- 不引入非 alembic 的裸 ALTER。
- `create_all()` 保留但退居兜底（空库/测试临时库）。

## 检查点0 验收（你手动）

1. 取一份**已有数据**的 rom_ai.db（dev 库即可）→ 启动 backend。
2. 断言：① 启动不崩、`/api/projects` 正常返回原有项目；② `DATA_DIR/backups/` 下有本次时间戳备份文件、大小非0；③ `alembic_version` 表 = head(`0022...`)；④ `DATA_DIR/repos/` 目录已建。
3. 负向：故意塞一个坏迁移 → 确认启动**中止并报错**、且**备份已在**（证明失败不毁数据）。

🔴 **停点**：0-A1(启动接 upgrade)、0-B1(REPOS_ROOT)、0-B2(备份) 三处属"新持久化/启动流程"NEW 面 —— **写完到此停，等你验收通过，才进检查点①**。

---

# 检查点 ① —— 建仓 → 选文件 → 一键清理（后做，检查点0 验收通过才动）

> 仅描述工作面。**起草阶段不写其实现代码。** 到 🔴 停点必停。

## D2 落地方式（加列不建表 + helper 收敛）

| # | 动作 | 文件 | 量级 | 标 |
|---|---|---|---|---|
| ①-D2a | `Project` 加列 `repo_root: str default ""`（1:1 时每项目一行，值=其仓库子目录绝对根）。**不建 repositories 表**。 | `models.py` | +1列 | 改 |
| ①-D2b | 把 `_managed_layout` 提升为公开 `repo_root_for(db, project)`（去 `_` 前缀、补 docstring 声明为唯一仓库根读取入口），建仓/staging/上传全走它。将来升 A 只改此函数。 | `project_files.py` | ~+5 改 | 改 |

## 动作1 建仓库

| # | 动作 | 文件 | 量级 | 标 |
|---|---|---|---|---|
| ①-1a | `POST /api/repos`：`REPOS_ROOT/{safe_slug(中文名)}` mkdir + 建 `_assets/`；重名 `exist_ok=False` → 明确 409/400 报错。复用 `sanitize_filename`。建 Project 行（复用 `_find_or_create_project`），回填 `repo_root`。 | `routers/`(建仓端点，可入 project_files 或新 repos 路由) | ~+40 NEW | NEW |

## 动作2 选文件/文件夹 + staging（🔴 staging 是 NEW 面）

| # | 动作 | 文件 | 量级 | 标 |
|---|---|---|---|---|
| ①-2a | **选路径**：pywebview `create_file_dialog`(FOLDER/OPEN) 拿绝对路径，经桥接给前端。**禁 web `<input type=file>`**（红线）。 | `desktop/launcher.py` + 前端桥 | ~+30 NEW | NEW |
| ①-2b | 拖放第二入口：pywebview 原生 drop 拿路径。**若实现麻烦本轮后置**，只做 2a 对话框（D3）。 | `desktop/launcher.py` | 视情况 | NEW(可后置) |
| ①-2c | **`POST /api/staging`（收料单）**：接收文件夹绝对路径 → 递归列文件、按文件夹分组、类型统计、**content_hash 去重标"已在库"**（依赖 0023）。只读盘不落库、不拷贝。dev 态提供**手输路径降级入口**先验流水线。 | `routers/staging.py`(新) | ~+80 NEW | 🔴 NEW |

## 动作3 一键清理（🔴 SSE + worker + 编排器 全 NEW）

| # | 动作 | 文件 | 量级 | 标 |
|---|---|---|---|---|
| ①-3a | **ingest 编排器**：串起五段 识别→抽取→切块→索引→归档，逐文件推进。复用 `parsing.parse_file`/`_store_file`/`content_chunks_json`/`retrieval.index_one`/`_extract_and_store_assets`。"向量"一律叫"索引"(FTS5)。 | `ingest.py`(新) | ~+120 NEW | 🔴 NEW |
| ①-3b | **N6 SSE 进度**：`GET /api/ingest/{job}/stream` StreamingResponse 逐文件逐段推。**禁同步转圈到底**。 | `routers/ingest.py`(新) | ~+50 NEW | 🔴 NEW |
| ①-3c | **DB-backed worker**：进度产生即写 DB（新表或 Project 字段），非 SSE 内存。支持"关闭重开续跑"。 | `ingest.py` + 0023 | ~+60 NEW | 🔴 NEW |
| ①-3d | 大文件抽图超时+失败降级：进"待重试区"不阻塞整体（复用 ocr.py 双预算范式）。 | `ingest.py` | ~+20 NEW | NEW |

## 0023 迁移（一次加齐 — 合并 content_hash + 续跑字段）

| # | 动作 | 文件 | 量级 | 标 |
|---|---|---|---|---|
| ①-0023 | 单个迁移 `0023_*`：`ProjectFile` 加 `content_hash: str default ""`(index) + 续跑进度字段（如 ingest job 状态表或 `ProjectFile.ingest_stage`）。`batch_alter_table + server_default` 双向。down_revision=`0022_admin_passcode`。 | `alembic/versions/0023_*.py` | ~+40 NEW | 🔴 NEW |

## 硬约束（贯穿检查点①）

- 全链路 pathlib+UTF-8；日志 `errors='replace'`；禁 bash 处理中文路径（已有 uploads.py 范式）。
- content_hash 计算（SHA256）在入库时，去重升级为 hash 优先、`_already_imported` 同名同大小兜底。
- 重复入库不产生重复 chunks（幂等键=content_hash）。
- dev 手输路径降级验流水线；exe 专项验对话框+中文路径。

## 检查点① 分层验收（你手动）

- **[dev]** 手输襄阳路径 → staging 列全量文件+分类+**36张重复标灰** → 一键清理见逐文件逐段进度 → 回执 chunks 数与库一致 **且带 truncated_at_page/total_pages 标注**（两者都满足才过）。
- **[dev · 灵魂]** 拖同批文件第二次 → 去重生效 → 中途关闭重开 → 续跑不重复。
- **[exe]** `create_file_dialog` 选襄阳文件夹（中文路径）→ 全流程走通。
- **36张标灰前置**：验收分两步——先入库自播种那36份，再拖同批验去重（否则 duplicates=0 该行不可能满足）。

🔴 **五个必停点**：N5 真切块(①-3a，本轮做不降级)、N6 SSE(①-3b)、ingest 编排器(①-3a)、0023 新持久化(①-0023)、staging 端点(①-2c) —— 到步必停等你。

---

## 执行节奏（对齐你的八条）

1. ✅ 盘代码、据实起草本清单 → **现在交你过目**。
2. ⏸ 你按老办法审（数新建栏 + 核改造/新建边界）→ 确认后我**先做检查点0**。
3. ⏸ 检查点0 你验收通过 → 才进检查点①，到🔴停。

**两笔记账保留、本轮不碰**：面板2秒漂移(独立bug)、项目名y位移瑕疵。

---

## 待你拍的两个起草期悬项

- **A. 续跑进度落 DB 的形态**：0023 里是①新建 `ingest_jobs` 表（job/file/stage/status），还是②给 `ProjectFile` 加 `ingest_stage` 列复用现表？我倾向**①新表**（job 维度天然，不污染 ProjectFile；文件级进度挂 job_item）。你定。
- **B. 建仓端点归属**：新建 `routers/repos.py`，还是并入 `project_files.py`？我倾向**新 `repos.py`**（职责清爽，与"仓库==项目"的 D2 语义对齐）。你定。
