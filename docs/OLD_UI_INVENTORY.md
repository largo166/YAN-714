# OLD_UI_INVENTORY — 旧 ROM-AI UI 盘点

> 来源(只读权威):`C:\ROM-AI开发V3-chatgtp\backend\ui\index.html`（2708 行单文件，Track A 主线 UI，PyWebview 加载）。
> 本文档为「原 UI 保真迁移」依据。视觉/交互以旧文件为准，不得擅自重设计。

## 1. 旧 HTML 主入口
- 唯一入口:`backend/ui/index.html`（CSS 内联于 7–605 行两个 `<style>`；body 607–1108；JS 1109–2572；设置抽屉 2573–2706）。
- 自包含，无外部 JS/CSS/图标资源；字体走 Google Fonts。

## 2. 页面 / 区域 / 功能入口（五板块）
顶栏导航 `#nav`（默认「项目中心」激活；「管理驾驶舱」默认隐藏，管理员登录后显示）:

| 板块按钮文案 | data-p | page id | 说明 |
| --- | --- | --- | --- |
| 项目中心 | proj | p-proj | 项目下拉/阶段进度/KPI/文件上传解析/AI研判5任务/里程碑/风险/会议纪要五段式 |
| 数据基地 | know | p-know | 检索(FTS5·BM25)/类型来源筛选/数据源台账/库存健康/文件树/可复用资产/效果图成果池 |
| 共创营地 | agent | p-agent | AI对话 composer/引擎+模式选择/积分/A2三卡生图参考/6 技能卡/成果卡回流 |
| 协作平台 | hub | p-hub | 团队成员卡(可编辑分工)/智能助手卡(找图小雷达等4个) |
| 管理驾驶舱 🔒 | boss | p-boss | 全员通知/跨项目KPI/工作量/飞书看板/项目评论(仅管理员) |

## 3. 整体布局结构
- 顶部 sticky `topbar`（brand 图标=ROM 笔画 SVG + 五板块 nav + 右上「●本地运行」）。
- 主体 `.wrap`（max-width 1180，居中），每板块一个 `.page`，`.page.on` 显示，切换带 fade 动画。
- 卡片网格:`.grid2/.grid3/.grid4`；卡片 `.card`，指标 `.metric`。
- 右侧滑出设置抽屉 `.setdrawer`（460px），点 brand 图标打开。
- 多个 `.modal` 弹窗（资产详情/会议/技能成果/图片预览），全局 toast / 二维码 / 后端不可达错误页。

## 4. 颜色系统（`:root` CSS 变量，**权威**）
- 底色 `--bg:#faf9f5`；面板 `--panel:#fff` `--panel2:#fcfbf6`。
- 墨 `--ink:#1b1a17` / `--ink2:#5e5a4e` / `--mut:#8c8775`。
- 线 `--line:#ebe7da` / `--line2:#e6e2d4`。
- 主色赤陶 `--terra:#c2703a`（soft `#c2703a14`，line `#c2703a40`）。
- 语义 `--ok:#788c5d`（橄榄绿）/ `--blue:#6a9bcc` / `--red:#b4534b`。
- 圆角 `--radius:14px`。

## 5. 字体 / 字号 / 间距
- 字体:`Space Grotesk`（主）+ `Noto Serif SC`（衬线 `.serif`）+ `Space Mono`（等宽 `.mono`，多用于路径/数字/技术标）。
- 标题 h1 24px/600；卡片标题 `.ct` 11px 大写灰色;正文 13–14px;行高 1.5。
- 间距:卡片 padding 18×20，网格 gap 14–18，页面上下 30/60。

## 6. 按钮样式
- `.btn` 墨底白字圆角 11px（hover 变深，带 sweep 扫光动画）；`.btn.ghost` 透明 terra 描边。
- `.anbtn` 研判任务按钮（hover/on 变 terra）；`.scope`/`.chip` 胶囊筛选;`.sw` 开关（on=terra）。

## 7. 卡片 / 面板样式
- `.card`（hover 上浮+阴影）、`.metric`（指标，v 26px，t=terra/r=red）、`.skill`（技能卡，含 ic 图标块/status 状态/run+done 态）、`.mem`（成员卡，头像 av）、`.sec`（知识库可折叠分区）、`.modal .panel`（弹窗）。

## 8. 设置页结构（右滑抽屉，9 组，**迁移重点不可简化**）
0. 管理账号（登录/登出）　1. AI 引擎与密钥（对话/生图/转写 + 默认引擎 + Mock 开关）　2. 知识库与数据（受管根目录/Obsidian Vault/DB 路径/自动重建/黑话词典）　3. 上传与文件（落盘目录/大小/分块/类型）　4. 项目与角色（默认项目/角色分工）　5. 数字员工与技能（4 个 Agent 开关）　6. 外观与语言（主题/语言/PPT 风格/默认板块）　7. 成果发送渠道（SMTP/企业微信/个人微信）　8. 隐私·同步·安全（本地运行/云同步/导出/清空）　9. 关于与系统（版本/仓库/后端/恢复默认）。
- 控件:密码框带 👁 掩码切换、`.sw` 开关、`.setsel` 单选胶囊、`.pathin` 路径框带「更改」。

## 9. 交互行为
- 导航切换页面;切到知识库/AI代理触发数据加载。
- 设置抽屉:brand 开 / ✕ / 遮罩 / ESC 关;开关 toggle;单选互斥;密钥眼睛切换。
- 共创营地:composer 的 +菜单 / @引用 / /命令 / 引擎下拉 / 模式下拉 / 拖拽上传;技能卡 待命→运行→完成;成果多选发送。
- 知识库:检索三触发(回车/放大镜/防抖)、scope 筛选、sec 折叠、文件树展开、效果图导入。
- 项目中心:项目下拉切换、文件上传解析、5 研判任务、发送/导出成果。
- 后端不可达 → `#srv-err` 全屏错误页（演示模式可忽略）。

## 10. 必须保留的内容
- 暖纸色 + terra 主色 + 三字体的整体气质;五板块导航与中文板块名(项目中心/数据基地/共创营地/协作平台/管理驾驶舱)。
- 设置抽屉 9 组结构与文案;各卡片/按钮/胶囊/开关样式;会议纪要五段式;A2 三卡;6 技能卡文案。

## 11. 可工程化重构、但不可改变视觉
- 内联 CSS → 抽为 `frontend/src/styles/legacy-ui.css`（已原样抽出，600 行）。
- 内联 JS 行为 → React 状态 + 事件;DOM 结构与 class 名保持一致以复用原 CSS。
- 写死的 PROJECTS / 成员 / 飞书看板演示数据 → 改为后端 API 或真实空状态。

## 12. 确实废弃 / 无效的内容
- 旧文件名「ROM-AI · 五板块」title;`ROM-AI.exe` 文案（新工程暂无 exe，改中性提示）。
- 旧绝对路径示例（E:\Rmo-Ai-00\...）仅作占位回填，非真实路径。
- 协作平台团队成员/智能助手为前端硬编码演示（迁移时标注，未接后端前按静态展示或空状态，不冒充真实数据）。
