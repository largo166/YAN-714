# 审计 · 前端完成度（2026-07-09）

> **基线**：HEAD `b5a9009`（2026-07-09，接力单封存 `9254444` 之后）。
> **方法**：只读静态审计（Explore 代理全扫 `frontend/src` + 人工 grep 三处核验），**未修改任何代码**。
> **形态口径**：静态代码审计，非运行时证据——凡涉观感/行为的存疑项仍需 dist 眼验，本档不替代。
> **范围**：seasky（海天OS，正式本体）+ pages（紫黑 legacy，已退役）。

## 总判断

seasky 数据链路（hooks→services→api.ts）**几乎全接真、三态齐备**：6 个 live hook 零 mock 零硬编码、零假按钮、零 TODO/FIXME。真问题两处：**(A) mock 文件全是死代码残留（引用者均为未挂载孤儿组件）；(B) `lib/api.ts` 约 24 个方法前端零调用，另一批能力只在 legacy pages 接线、未搬进 seasky 五板。**

## 1. Mock 数据残留 —— 全部死代码，无板块在跑假数据

| Mock 文件 | 被谁 import | 挂载? | 结论 |
|---|---|---|---|
| `broadcasts.mock.ts` | `system/BroadcastPanel.tsx:1` | 否 | 死代码；CockpitBoard:286-312 已用 `live.broadcasts` |
| `calendar.mock.ts` | `cockpit/ProjectCalendar.tsx:1` | 否 | 死代码；CockpitBoard:239-284 已用真实里程碑日历 |
| `usage.mock.ts` | `cockpit/UsageDonut.tsx:1`、`WorkloadPanel.tsx:1` | 否 | 死代码；CockpitBoard:188-237 已用 `live.usage/workload` |
| `projects.mock.ts` | `data/DataTypeTabs.tsx:1`、`data/KnowledgeCard.tsx:3` | 否 | 死代码；KnowledgeBaseBoard:104-128 已用真数据 |
| `skills.mock.ts` | 无（`campCards.ts:2` 注释标弃用） | — | 完全死代码 |

孤儿组件清单（可随 mock 一并删）：`UsageDonut` / `WorkloadPanel` / `ProjectCalendar` / `BroadcastPanel` / `DataTypeTabs` / `KnowledgeCard(RecentIntakeCard)`。

## 2. TODO / 占位符 —— 代码级 TODO/FIXME/HACK：0 处

出现的占位均为**诚实标注**，非欠功能伪装：SettingsOverlay 的"语义检索未接入/集成规划中"（:55/:438/:514/:535，只读状态展示）；boardImages/ProjectCenterBoard 效果图占位（等真图）；ImageAssetPool 缩略图失败回退。

## 3. 假按钮 —— 0 处

全 seasky grep 空 onClick / alert / console.log-only / disabled 永真：零命中。逐板抽查确认按钮均有真实副作用（CockpitBoard 发通知→`createBroadcast`；HubBoard 甲方画像→`getClientPortrait`、注释明示"全 app 唯一假按钮清零"；AgentCampBoard 快捷卡走预填→确认→`runSkill`；KnowledgeBaseBoard 四入口全真）。

## 4. 三态诚实 —— 活代码覆盖完整

抽查全部自取数组件（KnowledgeSearch / ProjectCenterBoard 两范本 + CockpitBoard / HubBoard / KnowledgeBaseBoard / MinuteFlowCard / OrganizeFlowCard / MeetingTile / GlobalSearch / ImageAssetPool）：loading/error/empty 三态齐。委托型组件（MoaView / AnalysisSummaryCard / SkillLibraryPanel 等）由父组件担三态，合规。唯一缺三态的是上述**未挂载**孤儿 mock 组件，不影响线上。

## 5. hooks 数据源 —— 六 hook 全接真

useProjectLive / useKnowledgeLive / useHubLive / useCockpitLive / useSkillsLive 全走 services→`/api/*`，逐源 allSettled 报错，无 mock 无硬编码。useBoardLive 为汇聚层无自取数。

## 6. api.ts 死方法与能力断层

**(A) 全库零调用（24 个）**：聊天模块 5（listChatSessions/createChatSession/getChatSession/deleteChatSession/sendMessage）；成果发送 2（getResultSendChannels/previewResultSend）；技能命令 3（runCommand/listSkillCommands/skillResultPptxUrl）；runAgent、createTeamAssignment；runReviewPrecheck/taskResultToAssignments/reflowMinuteToKb；getProject/deleteProject/getProjectFile/restoreProjectFile/createKnowledgeDoc；minuteFromFile/createTencentMeeting；health/extractBrief。

**(B) 只在 legacy pages 接线、seasky 无入口（新 UI 事实缺失）**：
- AI 研判**触发/导出/回流**（仅 ProjectAnalysisPanel）——seasky 只读摘要，不能发起研判
- 结构化认知编辑 extract/confirm/update/版本（仅 BriefCognitionCard/CognitionSection）
- 跨项目复用库三端点（仅 CrossProjectLibrary/BriefCognitionCard）
- 任务看板 listAssignments/updateAssignment（仅 TaskBoardPanel）——seasky 降级为待办计数
- getStageProgress / deleteProjectFile / reindexKnowledge / getFeishuBoard(设计即 not_configured) 等旧页专属

## Top 缺口清单（按严重度）

1. **成果发送 D1 完全没做**（两端点零调用，产品闭环最大缺口）
2. **AI 研判无法在新 UI 触发**
3. **结构化认知编辑未搬进 seasky**（P2 脊椎在新壳残缺）
4. **跨项目复用库缺入口**
5. **任务看板降级为计数**
6. **PPTX 导出按钮缺失**（skillResultPptxUrl 未接）
7. **智能体不可运行**（HubBoard 只列，runAgent 零调用）
8. **文件删除不可恢复 / 单文件详情缺失**
9. **腾讯会议 confirm-existing 与 minuteFromFile 未接**
10. **死代码清理债**（5 mock + 6 孤儿组件 + 聊天 5 方法）

## 人工核验补充（2026-07-09，grep 实证）

- `romai:projects-updated` / `romai:broadcast-updated` **已接线**（projectBridge.ts:61 / useHubLive.ts:77 / CleanupWizard.tsx:167 / useCockpitLive.ts:155）且有回归测试 `credibility.test.tsx` → **封板可信度包实际已执行**，其方案文件名"待批"过时。
- ModelSelector **仍是虚构三型号**：`seasky/lib/constants.ts:48` `MODELS = ['ROM Max · 深推理', 'ROM Pro · 均衡', 'ROM Lite · 快问快答']`，纯前端展示未接真映射（T1-6 拍板未决）。
- 类型筛选 UI **确无代码**：seasky 下 grep 类型筛选/typeFilter/filterType 零命中（P1-1 前端缺口属实）。
