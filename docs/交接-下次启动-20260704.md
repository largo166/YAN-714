# ROM-AI 交接 · 下次启动先看这个

> 生成时间:**2026-07-04 收工前**。权威库:`C:\ROM-AI-Claude 开发`(唯一真相源)。
> HEAD=**`ebae11b`**;alembic head=**`0022_admin_passcode`**;工作区干净(只剩 untracked 文档)。
> 本轮全是前端,后端零改动(上次后端状态:186 passed / 1 skipped + admin gate 测试)。
> **视觉权威=仓库根 `DESIGN.md`(已提交);过程档案=`docs/ui-2026-07-改版执行记录.md`。**

## 一、本会话做了什么(提交链,新→旧)

| 提交 | 内容 |
|---|---|
| `ebae11b` | **背景变色根修**(.sec/.card 平板色→玻璃)+ **背景统一两形态**(dots/aurora,sparks·orbits 弃用)+ **按钮标尺 v2 清扫 28 处**(一键创建腾讯会议→一键创建等) |
| `de0fde5` | **五板块首屏改版**:项目中心双链路入口卡+workzone 单开;数据基地类型 tile+最近5+文档抽屉(告别 640 条文件名墙);驾驶舱 gshell KPI 签名带(44px);协作 8s 淡入轮播+「即将上岗」行 |
| `cbf116a` | 晕动画热修:数据基地光丝/驾驶舱心电 → aurora 静谧光晕 |
| (更早) | a91e58b 板块动态背景+gshell / 177e3bf 字体统一 / cab6144 驾驶舱口令门禁 / a17b5fe 删 batch-ingest 孤儿管线 |

另:装了 5 套设计技能到 `~/.claude/skills/`(GSAP 官方×8、text-to-lottie、web-design、shadcn 官方、three-scope-map)。
⚠️ three-scope-map 是 **GPL-3.0+Vue 模板**,产码进 ROM-AI 前必须单独确认;shadcn 需 Tailwind,现栈不适用。

## 二、当前视觉体系(动 UI 前必读 DESIGN.md)

- **晕动症红线(不可回退)**:禁连续定向运动。在用背景只剩 dots(项目中心/营地)+ aurora(数据基地/协作/驾驶舱);streams/pulse/radar/sparks/orbits/.ticktrack 全部弃用保留。
- **首屏公式**:每屏一个主角;文件名长列表=首屏禁忌(全量进抽屉,首屏只留类型统计+最近≤5);工作面板默认收起、点 tile 单开。
- **按钮标尺 v2**:主按钮 36px/13px/700/字距.02em/每面板≤1;行内动作禁填充;文案≤6字全文进 title。
- **最大回归点**:项目中心 workzone 六面板**恒挂载 display:none 切换**——TaskBoardPanel 的 onRisk 喂本周聚焦数字,改成条件渲染会断数据流。
- 每页唯一 gshell:项目中心=脉搏卡/数据基地=库存脉搏/营地=composer/协作=甲方画像库/驾驶舱=KPI 带。

## 三、下一步任务清单(交接 TODO)

- [ ] **用户验收本轮改版**(刷新 5173 逐板块看;重点项目中心/数据基地)。反馈微调点直接提。
- [ ] **彩色 emoji 清理**(已挂后台任务芯片 task_f15f82d0):TaskBoardPanel 👤🐌 等→lucide 图标。
- [ ] **OCR(P0-1)收尾核实**:rapidocr-onnxruntime 安装当时在后台跑,**parsing.py 的 OCR 接线是否完成未核实**——下次先 `pip show rapidocr-onnxruntime` + 查 parsing.py 再下结论,勿直接宣称已完成。
- [ ] **重打一版 exe**(挂起):步骤见第四节;新列靠 `alembic upgrade head`(现 head=0022)。
- [ ] **自动分类**(挂起,等用户发话,勿自启)。
- [ ] (远期)锻造链 prompt 重造(compete 红海→concept 张力→judge 杀平庸→writer 独占句,需专门一轮)/追问系统。

## 四、怎么重打包 exe(参考)

```
cd "C:\ROM-AI-Claude 开发\frontend" && npm run build          # 先出前端 dist
cd "C:\ROM-AI-Claude 开发" && python -m PyInstaller desktop/romai.spec --noconfirm --distpath dist_exe --workpath build_exe
# 产物:dist_exe/ROM-AI.exe;冒烟 ROMAI_SMOKE=1(等 SMOKE_READY port=...)
```
- 关键文件:`desktop/launcher.py`(含收件箱 60s 轮询)、`desktop/romai.spec`、`backend/app/config.py`、`backend/app/main.py`。
- 真实 key 在 `backend/.env` + `desktop/.env.bundle`(gitignore,绝不进 git)。

## 五、起开发环境 + 红线 + 本机坑

- 后端:`cd backend && python -m uvicorn app.main:app --host 127.0.0.1 --port 8000`;前端:`cd frontend && npm run dev`(5173)。
- ⚠️ preview 浏览器常停在 **:8000 旧 dist** 造成"改了没生效"假象——先查 location 再下结论;换板块的 click 和读取要分两次 eval。
- ⚠️ HMR 编辑中间态会留陈旧报错(如 inboxOpen is not defined)——**tsc 是地面真值**,强刷后再判。
- ⚠️ `.venv` 必须有 PyMuPDF;数据基地无项目选择器(跟随全局 useProject);本机只有 `python` 没有 `python3`;经 Bash 传反斜杠转义串会被吞层(用 chr(92) 拼)。
- **工作口径**:不假按钮、只做确定的、最小改动、一写新管线就停、花钱动作只由用户触发、三态诚实。
- **工作方式(用户 2026-07-04 拍板,后续固定)**:Fable 先思考定案/标尺 → Workflow 派 agent 分桶执行(审计→修复,禁碰中央文件)→ 中央根修 Fable 亲手 → Fable 终审提交。
