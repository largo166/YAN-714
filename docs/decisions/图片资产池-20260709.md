# 图片资产池 · 已批裁决(2026-07-09 夜)

## 已批决议
1. **入口:两边都放**——项目中心「阶段拆解」卡头「图片资产 ›」(现状,已 dist 实测)保留;
   数据基地入口带加第三枚「图片资产 ›」(与「一键清理」同级),因数据基地板无 projectId,
   **抽屉顶部加项目选择器**(多一步交互,选项目后才拉该项目资产)。
2. **类型轴:asset_type 8 类(现状不变)**——render 效果图/plan 总图平面/reference 参考案例/
   model 白模体块/material 材质/logo/extracted 抽取/image 未分类。
   **不改 design_doc_type**:那是文档层 16 类,FileAsset 表无此字段;资产分类本就是 asset_type 干的活,
   硬挂 design_doc_type = 加字段+迁移+入库推断,超"纯前端+一 PATCH"边界(证据结案,不做)。
3. 后端零新增:GET /assets + PATCH /assets/{id}(改类型/软删,project_files.py:514 早在)+ thumb/image + reveal 全现成。
4. 主体组件已完成(ImageAssetPool.tsx,commit dc299fa);本轮增量=数据基地入口+项目选择器。

## 边界
纯前端 + 复用现有端点;不动 image_assets 抽图逻辑、不建新表、不动检索心脏。
