# 已批决议 · 项目识别修复 + 存量清洗（decisions log，带日期）

> 执行只认本文档,不认对话记忆。建议默认「待批」,用户明确批复才转「已批」并落档。
> 记忆漂移已发生 2 次(hunk 复活 / 存量口径记岔),本文档是纠偏机制。

---

## 2026-07-07 · 项目识别修复 总方针
- **已批**:A 为主语义 + B 为守卫,两者都做,不二选一。存量脏数据一并清。这是新功能+数据操作,**不并 hotfix 族**,单独成笔。
- **已批**:执行序 = 块2 守卫 → 块1 A语义 → 回归先行 → 块3 清洗后置。
- **已批**:出问题自动弹对话框让用户选修订,不静默。

## 2026-07-07 · 块1 A语义 待决①（散落文件）
- **已批**:忽略父目录直属散落文件,但向导显示提示行"该目录下有 N 个散落文件未归入任何项目,本次不入库"。忽略但不静默。

## 2026-07-07 · 块1 提交 + 确认屏验证
- **已批**:块1 提交,commit message = `feat(staging): 父目录按一级子目录识别为多项目 + 散落提示 + 名字警示`(按 agent 拟)。
- **已批**:确认屏验证走折中——不用 CDP 点 FolderPicker,**注入 staged multi 结果直接渲染 step2**,验四交互:①取消勾一个子目录→执行数相应减 ②全取消→"下一步"禁用 ③编号目录黄标在位 ④散落提示行显 N。截图交付。最终视觉用户在真 exe 终验(和清洗后一起一轮)。

## 2026-07-07 · 块3 存量清洗 —— ⚠️ 口径纠正（agent 曾记岔为"已批保留",实为待批）
**当前状态:归属未批,清单未拉。以下是已定的流程,不是已定的处置。**
- **已批(流程)**:先拉 14「数据基地」和 16「01_项目资料」的**全量文档清单(每篇带文件名 + 完整源路径)**给用户 → 用户**按源路径批归属**。
- **已批(判断方向)**:14 是**仓库根入库**,下面大概率多个项目文件**拍平混装**,处置**不是"改名保留",而是按路径前缀拆分、分别归属到真实项目** → 故 `reassign-files` 端点**大概率仍要做,不砍**。
- **已批**:12/13 先报**挂没挂文档**:挂了拉清单待批、空的批删。
- **已批**:17「襄阳资料」删(agent 测试建的)。8/9/11 保留(人工改名合法)。
## 2026-07-07 · 块3 三段处置（用户拍板，取代此前"待批"）
执行序:**先打功能包 → 去重+确定性操作 → 词典只读跑 → 覆盖率回来用户定**。

**第1段 确定性操作(脚本先过目,软删记日志):**
- **去重合并**:同名+同哈希副本只留一份,副本软删记日志(469 真实规模先缩)。
- **删 17**「襄阳资料」(agent 测试建)。
- **12/13 定案:都保留(真项目),按第1层命名改名** → 12=`襄阳-国投-建华望府`、13=`昆明-览悦`。**案名不确定必须停下问用户**,不擅自编。混入少量文件不现在抠,留词典轮。
- **14/16 先改名止血**:14→`未归类-收件箱堆积`、16→`未归类-桌面01目录`,摘掉伪项目身份;拆分等词典结果。

**第2段 词典匹配(只读先跑,数据定拆不拆):**
- 词典对去重后 14/16 文件聚类,输出分组映射表+覆盖率。
- **覆盖率≥60% → 用户按组批、迁移、残余挂未归类;<60% → 全挂未归类,精细归属后置 Phase 2**。跑前不预设结论。
- **词典表建正式库表,不写脚本常量**(后面喂 B 守卫 + 甲方画像归一)。初始关键词:石家庄=[市庄路,振三街,长安天曜,华药]、襄阳=[建华望府,国投]、昆明=[览悦]、上海徐汇、南京金茂、合肥万科、沈阳新希望、海南、武汉…

**第3段 打包:现在打,不叫 hotfix4,走版本号(internal-0.x)——功能包。**
- 内容:块1 A语义+确认屏、块2 守卫、认知资产笔。
- 张文说明主线:"入库入口升级——选父目录自动识别子项目、脏目录有守卫拦截,以后不会再长出'01_项目资料'这种项目。"
- 入口止血先发,存量清洗完成随下一包。出包铁律照旧。

## 2026-07-07 · 认知资产可移植性
- **已批(立项方向)**:认知回写+上下文供给作为 ROM-AI Phase 2 功能立项,把这半个月开发史写成第一个示范案例。
- **已批(本周)**:认知入库专项——CLAUDE.md/memory/决议全收进 repo,git 里没有的认知视为不存在。
- **已批(顺手)**:四份工作流 SOP 写成 skill 文件放 `.claude/skills/`,抄送 Obsidian 方法论库(脱敏单向流)。
- **已批(验证)**:干净机器/Mac Studio 到货做冷迁移演习——clone+拷 data,全新环境 Claude 接手任务,看是否遵守出包铁律、认得已批决议。演习通过才算闭环。
