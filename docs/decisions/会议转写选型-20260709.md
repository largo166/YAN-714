# 会议转写选型(2026-07-09 负责人裁决,已批)

> 来源:负责人调研笔记《技术选型笔记-Whisper接入会议纪要》(Downloads,外部文件)+ 当日裁决。
> 笔记本体不在仓库,本档即其认知资产化落位(认知回写铁律)。**执行只认本档。**

## 已批决议(2026-07-09)

1. **引擎 = faster-whisper**(Python 原生,CTranslate2)——**不用**命令行 whisper.cpp 二进制(否定笔记的"先试方式A")。
2. **模型 = large-v3**(建筑术语准度优先)——否定笔记的"先用 medium"。
3. **模型不打进 exe,首次运行下载**(体积红线;笔记§五-3 采纳)。
4. **pyannote 说话人分离,第一版带上**(区分甲方/我方)——否定笔记§四-5 的"第一版不做"。
5. **本地转写,录音不出本机**(红线,与笔记§六一致)。
6. **接进现有会议纪要 skill,不重写右侧逻辑**(五段式/黑话转译/对内外版全复用)。

## 代码现实:缝是预留好的(2026-07-09 审计)

- `models.Meeting.transcript_source` 注释即 `text|asr`(models.py:355)——asr 是设计位,从未通电;
- `Meeting.segments_json` 存 `[{text,speaker_key,start_ms,end_ms}]`(models.py:353)——正是带说话人分离的 ASR 产物形状;
- `meeting.build_prompt` 已按段消费 speaker_key(meeting.py:44)——分离结果直通五段式 prompt;
- `transcription.py` 整模块在场,docstring 明言"ASR provider 暂停使用,待后续确认 ASR 方案再启用"——本裁决即那个"后续方案";其云 ASR(openai/mimo)按本裁决**永不启用**(录音不出本机),本地引擎顶其位;
- 输入第四路:现有 贴文本(create_meeting)/传材料(from material)/腾讯会议同步 三路之外,加"上传录音"。

## 对笔记的三处覆写(防记忆漂移,显式记录)

| 笔记建议 | 裁决 | 理由 |
|---|---|---|
| 先试方式A(whisper.cpp CLI) | 方式B faster-whisper | Python 原生免二进制分发管理 |
| 先用 medium | large-v3 | 建筑术语准度优先 |
| v1 不做说话人分离 | v1 带 pyannote | 甲方/我方区分是纪要的核心价值 |

## 工程现实与开工前核实清单(AI 提请,未裁决部分)

1. **pyannote 依赖 torch**(体积大),不可随 exe → 分离栈走"首次启用时安装"或评估 sherpa-onnx 跑 pyannote 分割模型的 ONNX 版(复用已随包 onnxruntime,免 torch)——**开工首日实测二选一,不改变"v1 带分离"的裁决**。
2. pyannote 官方模型是 HF gated(需账号+token),国内网络有摩擦 → 镜像/离线分发方案要落。
3. **分离≠身份**:pyannote 输出 speaker-1/2 聚类,不直接给"甲方/我方"→ v1 落"分离+UI 可改名",不承诺自动认甲方。
4. large-v3 下载体积(约 1.5–3GB 视量化档,待核实)+ CPU 转写速度实测(1 小时录音耗时)→ 下载进度 UI + HF_ENDPOINT 镜像配置必须有。
5. faster-whisper 依赖 PyAV 解码 mp3/m4a(自带 ffmpeg 库,免系统 ffmpeg,打包收集待核实——同 onnxruntime 收集一族的坑)。
6. 许可证:faster-whisper/CTranslate2 MIT;whisper large-v3 权重 Apache-2.0;pyannote 代码 MIT、模型许可**单独确认**(笔记§五-2 纪律)。
7. **建筑术语加分项(AI 建议,待批)**:faster-whisper 的 initial_prompt 热词——把认知脊椎项目词典(项目名/甲方名/地名/常用术语)喂进转写,提专业词准度。认知脊椎直接变现,垂类护城河。

## 状态

选型已批,**实施方案待出、开工令待下**。主线路权不变。

## 已批决议·开工令(2026-07-09 深夜,选型定死直接实现)
1. **直接开工全套,不再分步等批**:faster-whisper + 说话人分离 + md/txt 读取入口,一次做全。
2. **模型改定 large-v3-turbo**:`deepdml/faster-whisper-large-v3-turbo-ct2`(覆写此前 large-v3 裁决——turbo 版速度数倍,准度接近);下载走 `HF_ENDPOINT=https://hf-mirror.com` 国内镜像;首次下载、不进 exe。
3. **分离栈定死 sherpa-onnx**(不用 pyannote):免 torch 2GB、复用已随包 onnxruntime、同 rapidocr 栈、绕开 HF gated。开工首日真录音实测中文分离效果,实在不行才回退 pyannote。输出 speaker-1/2/3 + 生成纪要前手动改名(→甲方/我方),不承诺自动认人。
4. **热词 initial_prompt 批,且是重点**:认知脊椎项目词典(项目名/甲方/地名/术语)喂转写——认知资产复利,建筑专业词准度是与通用工具的差异点。默认热词表(负责人给定):市庄/琅悦/中艺/江帆序/保利/展示区/示范区/归家动线/总图/户型/立面/抬板/架空层/会所/庭院/宋式/北方庄院/高台府门/连桥/下沉庭院/公共绿地/配套/货值/溢价/报规/强排/竖向/消防/日照/面宽/进深;后续允许项目级扩展。
5. **模型/依赖一律不进 exe,首次下载**(torch 若最终用到同理),exe 保持小。
6. **路权:转写(③)先做**,资产池小样/实施单独排。
7. **核实项边做边定**(sherpa 实测/镜像/体积),卡死才问,不为核实先停。
8. **验收=打包实测全链**:真实中文录音→转写(带项目热词)→说话人分离→改名→五段式纪要,exe 冻结态跑通,镜像下载真能触发;前端 build+后端测试过。

## P0 执行指令要点纳编(20260709-会议转写纪要入仓-P0-v01)
- 定位:会议录音→结构化项目资产(可检索/可归档/可追责/可复盘/可进看板)——"资料仓库"升级"项目过程操作系统"的关键一步。
- 六原则:本地优先(录音不出本机)/真实优先(失败如实报)/可用优先(spk-1/2/3+手动改名)/复用优先(parsing/纪要skill/project_id/知识库/看板,不另起系统)/项目隔离(全部挂 project_id,无项目先选不乱入库)/可验收(真实中文录音全链)。
- 两入口:A 录音(mp3/wav/m4a/flac/webm)→转写→分离→确认→纪要→入库;B 文本(txt/md)→复用 parsing→纪要→入库(微信纪要/飞书导出场景)。
- 异步十态:等待上传/上传中/检查文件/下载检查模型/转写中/分离中/生成纪要中/写入项目库/完成/失败(真实错误原因,不假成功)。
- 五段式产出锚定现有纪要 skill 结构;待办 0 条显真 0;检索目标:"曦总上次对庭院尺度怎么说""市庄展示区有哪些待办"。
- 边界不承诺:自动认人/声纹库/超长会议完美精度/100%专业词/直接可发甲方的正式纪要。
