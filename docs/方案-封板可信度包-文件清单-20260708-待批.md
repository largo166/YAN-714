# 封板可信度包 · 精确文件清单（T1三态诚实 + T2事件断链 + T4-5注释漂移）· 2026-07-08 待批

> 定位:数据可信叙事收尾,与蓝本"封板"阶段(D2 库状态视图)同包出。**不动蓝本主线、不新增大功能**——全部改动是"把已有真数据的失败/空态如实呈现 + 把断掉的事件接上"。
> 心脏判定:**不涉检索/入库写路径**(useProjectLive/useHubLive/useCockpitLive 是只读展示 hooks;两个新事件只触发重拉)。回归=组件测试+CDP 截图,不需心脏链路全跑。
> T1-6(模型选择器)与 T4-1(收件箱)按批复**不在本包**,另出小方案。

## 一、改动文件清单(11 文件,预估 ~180 行)

### T1 三态诚实(7 文件)

| # | 文件 | 改动点 | 预估行数 |
|---|---|---|---|
| 1 | `frontend/src/seasky/hooks/useHubLive.ts` | 已收集的 `errs`(:40-49)聚合为对外 `err: string\|null`(四源任一失败→"成员/通知加载失败:…"),接口加字段 | +8 |
| 2 | `frontend/src/seasky/components/boards/HubBoard.tsx` | ① 板内渲染 `live.err`(照 b0 模式,红字一行);② 智能助手列空态(:133-146):"暂无智能体。等待接入。";③ 甲方画像 pill 接线(:118-125):onClick→Popover 显 `getClientPortrait`(端点已有,services/index.ts:60 已挂)——**全 app 唯一假按钮清零** | +45 |
| 3 | `frontend/src/seasky/hooks/useCockpitLive.ts` | 接口加 `err: string\|null`(:16-28);allSettled 四源(:101-104)失败→记 err;日历里程碑失败(:122-123)→计入 err 不再静默 | +12 |
| 4 | `frontend/src/seasky/components/boards/CockpitBoard.tsx` | 渲染 `live.err`(门禁后主区顶部一行);有 err 时空态文案让位(错误≠空库) | +10 |
| 5 | `frontend/src/seasky/hooks/useProjectLive.ts` | ① projectId==null 时置 `loading:false`(:26 修 loading 永挂);② 逐源记错(:47-50 改:任一源失败即拼进 err,不再"全败才报") | +10 |
| 6 | `frontend/src/seasky/components/boards/ProjectCenterBoard.tsx` | 无项目时数字区显"—"(承接 loading:false);部分失败提示(live.err 已渲染:53,措辞适配"部分数据加载失败") | +6 |
| 7 | `frontend/src/seasky/components/agent/SkillLibraryPanel.tsx` | 空技能列表(已加载且为空,:51)补:"暂无技能。检查后端连接后重试。" | +4 |

小项(并入上述文件的 diff,不另立):
- `KnowledgeBaseBoard.tsx:48` — `live.typeStats.length || '—'` → `live.loading ? '…' : String(live.typeStats.length)`(真 0 显 0)
- `MeetingTile.tsx:156` — 空态句式补指引:"本项目还没有会议记录。点击上方磁贴创建第一场会议。"

### T2 事件断链(4 文件,新增 2 个事件)

| # | 文件 | 改动点 | 预估行数 |
|---|---|---|---|
| 8 | `frontend/src/seasky/components/data/CleanupWizard.tsx` | ① 入库完成派发 `romai:projects-updated`(与 :164 knowledge-updated 并排——入库可能建新项目 ingest.py:103);② 删死派发 `romai:seasky:recent-refresh`(:364,全仓零监听) | +3/-2 |
| 9 | `frontend/src/seasky/services/projectBridge.ts` | useProjectBridge 监听 `romai:projects-updated` → 重拉项目列表(reload 已有,:35-55 挂事件即可)——**修"入库新建项目切换器看不见"真 bug** | +6 |
| 10 | `frontend/src/seasky/hooks/useCockpitLive.ts` | (与 #3 同文件)sendBroadcast 成功后派发 `romai:broadcast-updated`(:137-140) | +2 |
| 11 | `frontend/src/seasky/hooks/useHubLive.ts` | (与 #1 同文件)监听 `romai:broadcast-updated` → 重拉 broadcasts——修"驾驶舱发通知协作板横幅不更新" | +6 |

事件命名约定同步(CLAUDE.md「事件命名约定」节追加两行):
- `romai:projects-updated` — 项目列表变更(入库新建项目)。消费者:projectBridge(项目切换器)。
- `romai:broadcast-updated` — 通知/广播变更(驾驶舱发布)。消费者:useHubLive(协作板横幅)。

### T4-5 注释/文案漂移(4 处,纯注释+1 处防御)

| 文件 | 修什么 |
|---|---|
| `frontend/src/seasky/components/agent/CleanupFlowCard.tsx:5` | "CleanupOverlay 是唯一权威操作面"→"CleanupWizard(b1 三步向导)是唯一权威操作面" |
| `frontend/src/seasky/components/system/SettingsOverlay.tsx:95` | 三消费者注释里 CleanupOverlay→CleanupWizard 口径 |
| `backend/app/routers/review_checklist.py:3` + `frontend/src/lib/api.ts:656` | "功能/甲方/成本三位专家"→"设计总监/空间设计师/形式设计师三视角(设计版)" |
| `frontend/src/lib/api.ts:536` + `backend/app/ingest.py:5,37` | "五段流水线"→"四段流水线(落盘/解析/入库索引/归档抽图)"——对齐 STAGES 实际 4 段 |
| `backend/app/routers/review_checklist.py:207` | history 端点 `json.loads` 包 try/except(照 :180-183 同款兜底)——顺手防御,防脏数据 500 |

## 二、明确不做(本包边界)

- 不动 `useKnowledgeLive` / 检索 / staging / ingest 写路径(心脏零触碰)。
- 不做 T1-6 模型选择器(待拍板)、T4-1 收件箱(待拍板)、T3 死代码清退(待拍板)。
- 不删 CleanupOverlay 组件本身(属 T3 清退范围),只修指认它的注释。
- 不动锁版视觉面;新增 UI 仅:错误提示行(既有红字样式)、空态文案(既有句式)、甲方画像 Popover(既有 Popover 组件复用)。

## 三、测试与验收

**组件测试(vitest,新增 ~6 用例):**
1. useHubLive:mock 一源 reject → `err` 非空(错误不再静默)。
2. HubBoard:传入 err → 板内出现失败行;agents 空 → 空态文案。
3. useProjectLive:projectId=null → loading=false;单源失败 → err 含该源名。
4. projectBridge:派发 `romai:projects-updated` → listProjects 被再次调用。
5. useHubLive:派发 `romai:broadcast-updated` → listBroadcasts 被再次调用。
6. CleanupWizard 既有 7 用例回归(断言新增 projects-updated 派发)。

**CDP 截图验收(build dist 形态,标注哈希):**
- 后端关停下打开 b3/b4 → 板内可见失败提示(不再是干净空态)——错误可见性的"前后对照"照。
- 正常数据下 b0(无项目)→ 数字区"—"非"…"。
- 走一遍 CleanupWizard 入库含新项目目录 → 不重启,项目切换器出现新项目(T2-1 修复实证)。

**秒表口径:** 无性能敏感点,不设秒表;验收以"错误可见/空态有指引/两事件闭环"三查为准。

## 四、涉险声明
- `useProjectLive` 改"逐源记错"会让 b0 在单源失败时显示错误行——**行为变化是本意**(错误伪装成空是要修的病),但若后端某端点长期 500 会常驻红字,属如实暴露非回归。
- 甲方画像 Popover 依赖后端 `GET /api/clients/{name}`(clients.py:29 已有),若无画像数据返回 404 → Popover 显示"暂无画像数据。"(三态齐,不伪造)。
