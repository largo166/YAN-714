# 规划 vs 落地审计（2026-07-09 刷新版）

> **基线**：HEAD `21e3eb5`（比两份前审计基线 `b5a9009` 前进 2 笔：`691ccea` 落档双审计+四裁决、`21e3eb5` 撤模型选择器）。
> **方法**：只读文档审计 + 代码标记核验（静态 grep `frontend/src`+`backend/app`，逐项带 file:line；属静态证据非运行时，观感/行为项仍需 dist 眼验）。
> **口径纪律**：文件名"待批"多已过时（会议转写、封板可信度包实际已 packaged/执行）；判断以「已批决议」标记 + 代码为准，**不以文件名为准**。

## 刷新性发现

自审计基线以来，`b5a9009..HEAD` 的代码改动**只有"撤模型选择器"一项**（`ModelSelector.tsx` 删除 + `constants.ts`/`AgentComposer`/`AgentCampBoard` 改）。**B④B⑤、P1-2/3/5 等"已批"项在 HEAD 上零代码落地**——批复在 `691ccea` 落档，实现尚未开工。**"已批"与"已落地"之间隔着整整一个待做清单。**

**主控复核确证**：`sk-skeleton|Skeleton` 在 seasky 树 **0 文件命中**；hooks 里唯一 `reload` 是 `useHubLive.reloadMembers`（成员增删用），**无任何板级 `reload()`/`retry`**；`git log b5a9009..HEAD` 仅两笔。

---

## 一、已批但未完成

| 功能名 | 所属板块 | 来源 `doc:line` | 现状一句话 | 前端相关 |
|---|---|---|---|---|
| **B④数据层契约 + B⑤骨架屏** | 全五板 hooks | `方案-B4数据层统一-B5骨架屏-待批.md:167-172`（已批:骨架1.2s低对比,验收含"断网必现重试态"） | **零落地**——seasky 树 0 个 Skeleton,hooks 无板级 reload/retry,四板只有失败文案无重试按钮 | **是**（本轮实现） |
| **P1-3 入库决策点压缩** | 数据基地·入库 | `方案-P0+…-待批.md:182-183`（已批:P1-3优先放行） | 完成页只有"去看最近入库"(仅关闭),无"去检索该项目"直达/预填,FolderPicker 无常用目录记忆 | **是** |
| **P1-1 类型筛选UI+手动改类型** | 数据基地·检索 | `decisions/前端能力增量路线图-20260709.md:21`+`方案-P0+…:177`（已批5:筛选UI要做先小样） | 16类分组已切(`GlobalSearch.tsx:34-37`),但**筛选下拉、文档卡改类型菜单缺失**(改类型仅图片资产有) | **是** |
| **P1-5 检索历史** | 数据基地·检索 | `方案-P0+…:183`（已批,纯localStorage） | `recentSearchProject` 只记"最近@项目"(重名消歧),**非检索词历史** | **是** |
| **图片资产池·数据基地入口项目选择器** | 数据基地 | `decisions/图片资产池-20260709.md:5-6`（已批:抽屉顶加选择器） | 第三枚入口已落,但抽屉无 projectId 时**只显"请先选择项目"文字、无下拉**→**点开是死路(假入口)** | **是** |
| **任务#34 A语义前端确认屏** | 项目中心·入库 | `接力-下次开机起点-20260709.md:29`+`decisions/项目识别修复与存量清洗.md:18`（后端已补,前端确认屏未做,明确挂起） | 未落地 | **是** |
| **P1-2 命名建议预览表** | 数据基地·入库 | `方案-P0+…:183`（已批,守§7.10禁清单） | 后端 naming.py 全套+前端 NamingPreview 全不存在(现有 project_naming.py 只是拒名守卫) | **部分** |
| **P1-4 content_hash 精确去重** | 数据基地·入库 | `方案-P0+…:184`（已批,**触库管制**:先交0023迁移+回滚方案再动） | 未落地且**按闸门正确未动**(迁移方案未产出) | 否 |
| **数据基地2.0 P2–P5 主线** | 数据基地 | `decisions/数据基地2.0蓝本.md:159-169`（已批蓝本,逐阶起令） | P0封板/P1刚放行;P2版本链·P3引用回滚·P4大纲分页·P5 MCP+DNA 全未开工 | **部分** |
| **项目级健康格/评分制** | 数据基地·驾驶舱 | `数据基地2.0蓝本.md:89-92`+`锚点核验表:40-45` | 仅落 P0 全库 health pill;项目级评分+建表推迟(权重未定,建表=空表) | **是** |
| **库完整性校验 Phase 2** | 数据基地 | `decisions/库完整性校验方案.md:33-36`（第二级后置） | Phase1已落(215 passed);Phase2 健康环卡、检索"⚠文件缺失"标注、一键修复未做 | **部分** |
| **五板块粒子背景·观感验收** | 全五板 | `decisions/五板块粒子背景-20260709.md:3-22`+`接力:19` | **机制已落**(统一FilmSea/三档/ParticleField已删),**仅待 dist 眼验三标准**——无剩余代码工作 | 是(仅验收) |
| **真库存量清洗** | 数据基地 | `decisions/项目识别修复与存量清洗.md:45-50`（口径已批但**中止**:开发库是死库） | 阻塞于真库体检,`cleanup_stock.py` 封存待用 | 否 |

**已对账闭合（刷新前审计,不再算未完成）**：T1-6 撤型号选择器（`21e3eb5`）、封板可信度包 T1+T2（甲方画像已接线+两事件闭环）、P0检索封板、**会议转写本地录音全链**（packaged ALL_PASS，`方案-会议转写实施-待批.md` 文件名过时）、开机影片v12（packaged四断言）、④斜杠命令+技能卡+结果卡进对话流、T0-1/T0-2/T0-3、库完整性 Phase1。

## 二、待批（规划中）

**A. 全栈总路线剩余（`方案-全栈提升总路线-20260708-待批.md`）**
- **T3 紫黑pages清退**（17文件+api.ts瘦身69方法）/全局·死代码/`:50-52`/未决:继续冻结or清退(连带5mock+7孤儿组件)/前端:**是**（已明确单独拍板，不在本轮）
- **T4-1 收件箱自动扫描**/数据基地/`:58`/二选一:60s轮询 or 文案回撤+seasky加"立即扫描"按钮(现状:配了收件箱文件永远堆着pill不入库)/前端:**部分**
- **T4-3 MoA成本落库**/驾驶舱/`:60`/未决:出ai_usage表、用量卡从"数次数"升"显费用"/前端:**部分**
- **T4-4 MoA三暗预设**（concept/compare/compete_moa）/共创营地/`:61`/未决:给开关or明档"API-only"(现API可达UI无入口)/前端:**是**

**B. 通用层技术选型（`方案-通用层技术选型对照-20260709-待批.md`,已批决议段空 `:44`）**
- **报告出口HTML→PDF**/数据基地/`:18-27`/待批:推荐复用 build_print_html 走WebView2打印/前端:**部分**
- **大纲/章节提取（唯一真缺口）**/数据基地/`:28-32`/待批:进P1排P1-2之后,后端fitz.get_toc零使用/前端:**部分**

**C. 前端增量"等叫"项（`decisions/前端能力增量路线图-20260709.md:43-44`）**
- **① 大纲索引前端**（渐进披露）/数据基地/`:44`/后端outline提取未接/前端:**是**
- **② OCR"含OCR文字"小标记**/数据基地/`:23`/文件/图片卡无OCR角标/前端:**是**
- **⑤ PPTX导出/打印按钮**/共创营地·成果卡/`:38-39`/**后端端点已在**`skills.py:734`,前端从不调用/前端:**是**

**D. 共创线（封冻/已否——非待批,是明确不动）**
- **概念锻造链forge台UI**/`方案-共创营地改版-概念锻造链-20260708-待批.md:59-62`/**已否**(C-P0盲评3:0败)/前端:是(但不做)
- **C-P1锻造链回路**/`方案-C-P1锻造链回路机制-20260708-待批.md:1-7`/**已裁搁置**转能力储备/前端:否
- **专业方案批评引擎（七视角）**/`方案-专业方案批评引擎-设计草案-20260708.md:90-99`/**封冻**(样本4/15未到量+盲评门未过,<10不上线)/前端:是(但不做)
- **judge语义迁移命名**/`命名建议-judge语义迁移三方案-20260708-待批.md:3`/**搁置**随锻造链/前端:否
- **AI自动建筑判断进产品**/`decisions/共创线-唯一约束.md:3-8`/**硬封冻**(人填卡≥N+盲评双门齐过才解封)/前端:是(但不做)

**E. 审计发现的前端缺口（不在任何方案文档,属未归属backlog）**——与"前端完成度审计" Top 清单同源:成果发送D1、AI研判触发/导出/回流、跨项目复用库入口、任务看板、智能体运行runAgent、文件删除可恢复/单文件详情、腾讯会议confirm-existing/minuteFromFile。

## 三、前端向未完成项汇总（四桶）

### 🔴 必须先补（已批+高影响,缺则违DoD/铁律或断核心链路）
1. **B④B⑤ 数据层契约+骨架屏**——已批,且"断网静默空态"直接违"三态诚实"DoD;**是当前 `polish/three-states` 分支的本职工作,零代码是最扎眼落差**。→ **本轮实现**
2. **P1-3 入库决策点压缩**——已批且点名"优先";入库完无法直达检索该项目,闭环缺口。
3. **图片资产池·数据基地项目选择器**——已批;缺它则那枚已上线入口点开是死路(**假入口**,踩"不假按钮"红线边缘)。→ **下一批 HTML 小样候选**
4. **任务#34 A语义前端确认屏**——后端已就绪;NL"预填+一键确认"是不可降级铁律,缺确认屏=入库判断无人工闸。

### 🟢 可顺手补（已批/低成本,纯前端或后端已就绪）
5. **P1-5 检索历史**——已批,纯localStorage,无后端依赖。
6. **P1-1 类型筛选下拉+手动改类型**——已批(先小样);16类分组已落,仅差控件+菜单。
7. **⑤ PPTX导出/打印按钮**——后端 `skills.py:734` 已在,前端一个按钮接线。→ **下一批 HTML 小样候选**
8. **② OCR小标记**——文件/图片卡一个角标,几无新UI。

### 🟡 暂缓（已批但排期靠后/被闸门管制/待上游/仅待验收）
9. P1-2命名建议预览(需先建后端naming.py全套,排P1-3后) · 10. P1-4 content_hash(触库管制,先交迁移方案) · 11. 数据基地2.0 P2–P5前端(逐阶起令) · 12. 项目级健康格/评分(权重未定,建表=空表) · 13. ①大纲索引前端(本轮明确搁置,后端outline未接) · 14. 五板块粒子背景观感(机制全落,仅待dist眼验,agent不自调参) · 15. 共创卡容器/认知写回(已解管制但属重管线,先后待裁) · 16. T4-1立即扫描/T4-3费用卡/T4-4 MoA开关(待拍板后端二选一) · 17. 审计缺口组(无归属方案,先立项定范围)。

### ⚫ 不建议做（冻结/已否,此刻投前端即浪费）
18. 概念锻造链forge台UI(盲评3:0已否) · 19. 七视角批评引擎UI(封冻,<10样本不上线) · 20. judge语义迁移UI(随锻造链搁置) · 21. AI自动建筑判断入口(硬封冻,双门齐过才解封,碰即停) · 22. T3紫黑pages清退前端(待拍板,冻结策略在案可回滚,非阻塞,不急动)。

---

## 两条铁律附注

① **出包铁律触发**：工作区 4 份"已批决议"文档（B4B5/P0+P1/全栈总路线/FUTURE_CAPABILITY_POOL）有**未提交增量**（批复主体已 `691ccea` 入库，工作树另有细化未 commit），外加研究员落盘的 `docs/research/ppt/…`。按出包铁律第 1 条，**出包前需先归属提交**——现在不阻塞审计，但下次打包前必须清。

② **证据形态**：本报告代码判定均为**静态 grep**；粒子背景观感、P0+ 复制/reveal 等**行为/观感项的最终结论仍须 dist/packaged 眼验**——不在 dev 下结论。
