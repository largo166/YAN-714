# 前端完成度审计（2026-07-09 刷新版）

> **审计口径**：`frontend/src/seasky`（上线 UI）。`frontend/src/pages` 是旧紫黑退役页，仅作能力参照，**不计入"用户可达"**。
> **方法**：两条证据流（A=mock/TODO/api 未接线；B=假按钮/三态/hooks）逐项 grep+读码，重结论由主控本人 grep 复核。
> **基线**：HEAD `21e3eb5`。旧审计 `docs/audit-前端完成度-20260709.md`（注：旧文件名无 `audit/` 子目录）仅作对照，结论只认当前代码。
> **证据形态**：本报告代码判定均为**静态 grep 证据**；观感/行为项（粒子背景、复制/reveal 等）最终结论仍须 dist/packaged 眼验，不在 dev 下结论。

## 一句话结论

**seasky 的"诚实度"极高——上线 UI 假按钮 0、进看板的 mock 数据 0、裸 `fetch` 0、`TODO/FIXME` 注释 0、6 hooks 无假数据兜底。** 真正的"未完成"不是烂尾，而是**后端能力跑在了前端入口前面**：`api.ts` 126 个可调方法里 **55 个在 seasky 无任何调用点**——一批已建好的后端能力（任务看板/认知回路/研判历史/成果发送/知识文档管理）**在新版 UI 里没有入口**。这才是补齐重点，而非"修 bug"。

> ⚠️ 严格区分：下列"未接线"= **后端有能力、前端无入口**，**不等于"产品已完成"**，也不等于"旧紫黑页有就算能点到"。

---

## Cat 1 · mock 数据残留

| mock 文件 | 被谁 import | 结论 |
|---|---|---|
| `projects.mock.ts` | `DataTypeTabs.tsx:1` / `KnowledgeCard.tsx:3` | **孤儿**（两个 importer 均未渲染） |
| `calendar.mock.ts` | `ProjectCalendar.tsx:1` | **孤儿** |
| `usage.mock.ts` | `UsageDonut.tsx:1` / `WorkloadPanel.tsx:1` | **孤儿** |
| `broadcasts.mock.ts` | `BroadcastPanel.tsx:1` | **孤儿** |
| `skills.mock.ts` | 无人 import | **完全死文件**（`campCards.ts:2` 注释已标"已弃用"） |

- **5 块看板 100% 跑真数据**（`useCockpitLive/useHubLive/useKnowledgeLive/useProjectLive/useSkillsLive`）。**零 mock 进看板。**
- `boardImages.ts`、`campCards.ts` 是**合法静态配置**（背景图路径 / 快捷卡定义带真 `skillId`），非假业务数据，不算残留。
- **复核确证**：`UsageDonut|WorkloadPanel|ProjectCalendar|BroadcastPanel|DataTypeTabs|KnowledgeCard|CleanupOverlay` 在整个 `frontend/src` 只匹配各自 `export function` 定义行，零导入/零渲染；`AppShell.tsx:185-214` 只挂 5 块看板 + Settings + GlobalSearch。

## Cat 2 · TODO/FIXME/占位

- **seasky 内无任何 `TODO/FIXME/WIP/coming soon` 注释标记**（所有 "todo" 命中都是名词 `todos` 计数字段）。
- **上线代码里 3 处诚实占位**（明确标注+禁用，非假按钮）：
  - `ImageAssetPool.tsx:326-333`「用于报告」禁用（注"本轮不实现"）
  - `SettingsOverlay.tsx:535` 集成页"对外供给·规划中"
  - `SettingsOverlay.tsx:53` 规划行徽标
- 残留 "即将接入" 字符串只存在于**死 mock 文件**（`broadcasts.mock.ts:5` 等），用户不可达。

## Cat 3 · 假按钮

- **上线 UI：0 假按钮**（每个 `onClick` 接真 API/状态，逐个已验）。
- 唯一真假按钮 `BroadcastPanel.tsx:14`「发布」无 `onClick` —— 属**孤儿死代码**，真入口在 `CockpitBoard.tsx:299`。
- 小瑕疵（非假按钮）：`TopNavCapsules.tsx:60` StatusBar 硬编码 "Cockpit Unlocked"，与实际闸门态无关。

## Cat 4 · 三态诚实（12 组件抽查）

**9/12 三态齐全**（含"暂无X+下一步"句式）。**3 处缺口**：

| 组件 | 缺 | 证据 | 风险 |
|---|---|---|---|
| `CockpitBoard` | **loading** | 数据格加载中甜甜圈显"0 次成果"无图例，像空态不像加载 | 中 |
| `SettingsOverlay` | **error** | `:149-183` `Promise.allSettled` 吞 reject，stats/health 失败永久显"…" | 中 |
| `KnowledgeBaseBoard` | **empty 文案** | 类型带 0 时不渲染，缺"暂无资料类型"句 | 低 |

> 三态是新板 DoD 硬性项——前两条纳入"必须先补"。

## Cat 5 · hooks 数据源

**6 个 hooks 全部干净**：只经 `services/*`（`api.ts` 薄封装）取数，**零 `*.mock` import、零硬编码数组、零失败兜底假数据**，失败均出诚实错误串。事件订阅到位：
- `useKnowledgeLive` 听 `romai:knowledge-updated`+`romai:settings-updated`
- `useHubLive` 听 `romai:broadcast-updated`
- `useCockpitLive` 发 `romai:broadcast-updated`

## Cat 6 · api.ts 未接线（55/126）

**71 接线 / 55 未接线 / 0 裸 fetch 绕过。** 死能力聚类：

| 聚类 | 未接线方法 | 说明 |
|---|---|---|
| **任务看板** | `listAssignments` `updateAssignment` `taskResultToAssignments` | 后端全建好，前端无板 |
| **P2 认知回路** | `extractModule` `confirmCognition` `updateCognition` `listCognitionModules` `listCognitionVersions` | 营地只读 `listCognition/querySlang`，不能抽取/确认/改/看版本 |
| **跨项目沉淀** | `listCrossProjectTypes` `listCrossProjectLibrary` `precipitateToCrossProject` | B 类复用库无前端 |
| **AI 研判** | `analyzeProject` `listProjectAnalyses` `analysisExportUrl` | `latestAnalyses` 已接（只读最新），但**不能触发新研判/看历史/导出** |
| **成果发送** | `getResultSendChannels` `previewResultSend` | 汇报出口无入口 |
| **知识文档** | `getKnowledgeDoc` `createKnowledgeDoc` `deleteKnowledgeDoc` `generateDocMetadata` `reindexKnowledge` | 只看统计，不能管文档/重建索引 |
| **PPT/斜杠命令** | `skillResultPptxUrl` `runCommand` `listSkillCommands` | 成果导 PPT、斜杠命令均无入口 |
| **其它** | `restoreProjectFile` `reflowMinute` `minuteFromFile` `createTencentMeeting` … | 零散 |
| **专用端点被绕过**（非缺口） | `runMoaReview` `getMoaReview` `runReviewPrecheck` | 功能走 `runSkill('review',…,'moa')` 活着，仅死方法 |
| **按设计已退役**（非缺口） | 5 个 chat-session 方法、`getFeishuBoard` `getBossComments` | 营地用技能非聊天；boss 桩返回 not_configured |

**复核确证**：`analyzeProject|listAssignments|previewResultSend|runMoaReview|confirmCognition|precipitateToCrossProject|runCommand|skillResultPptxUrl` 等 20 个代表性方法名在 seasky 全树 grep → 0 命中；`AgentCampBoard.tsx:521` 传 `'moa'`、`:147` 走 `cs.runSkill(..., mode)`、`:254` 解析 `output_json`——MoA/评审功能活着、专用端点被绕过。

---

## 前端未完成功能 Top 清单（按严重度）

每项：**影响的用户路径 / 已有后端能力 / 新版 UI 缺什么 / 建议怎么补**

**① 任务看板落地　🔴 高**
- 路径：会议纪要审定后 → 追踪任务。`MeetingTile.tsx:153`「任务看板」是**不可点 `<div>`**，副标"纪要审定后回流生成"——**UI 承诺了、无板可去**。
- 后端：`listAssignments`/`updateAssignment`/`taskResultToAssignments` 全备（`api.ts:646-672`）。
- 缺：一块 todo/doing/done 看板 + 纪要→任务落板入口。
- 补法：新建轻看板组件（可挂 HubBoard 或项目中心页），接三方法；`MeetingTile` 那格改成真入口。三态齐全。

**② P2 结构化认知回路（抽取→确认→修订→版本）＋跨项目沉淀　🔴 高**
- 路径：营地里对项目"抽取甲方诉求/地块条件→人工确认→沉淀成跨项目复用"。现状 `AgentCampBoard.tsx:116/142` **只读** `listCognition/querySlang`。
- 后端：`extractModule/confirmCognition/updateCognition/listCognitionModules/listCognitionVersions` + `listCrossProjectTypes/listCrossProjectLibrary/precipitateToCrossProject` 全备。
- 缺：认知卡的"抽取/确认/编辑/看版本"动作 + 跨项目复用库视图。
- 补法：认知面板加"预填→人工确认"回路（**严守预填+确认红线**）；跨项目库单开一视图。⚠️ 体量大，先拍板范围。

**③ AI 研判：触发 + 历史 + 导出　🟠 中高**
- 路径：项目中心里主动发起研判、回看历史、导出 md。现状 `useProjectLive.ts:42` 只回填**最新**结果。
- 后端：`analyzeProject/listProjectAnalyses/analysisExportUrl` 全备。
- 缺：研判触发按钮 + 历史列表 + 导出链接。
- 补法：`ProjectCenterBoard` 研判区加"重跑/历史/导出"，接三方法。

**④ 成果发送 / 汇报出口入口　🟠 中**
- 路径：成果 → 选渠道 → 预览发送（当前仅 preview/not_configured，不真发）。seasky 无任何入口。
- 后端：`getResultSendChannels/previewResultSend` 备。
- 缺：成果卡上的"发送预览"动作。
- 补法：成果卡加发送预览弹层，明确"预览不真发"话术。

**⑤ 知识文档管理 + 重建索引　🟠 中**
- 路径：数据基地里管理单篇文档、生成元数据、重建索引。现状只看统计/最近入库。
- 后端：`getKnowledgeDoc/createKnowledgeDoc/deleteKnowledgeDoc/generateDocMetadata/reindexKnowledge` 备。
- 缺：文档级管理面 + 重建索引按钮。
- 补法：`KnowledgeBaseBoard` 加文档抽屉 + 一个"重建索引"动作（带三态）。

**⑥ 报告配图 / 成果导 PPT　🟠 中**（与外部研究 EasyMap 闭环强相关）
- 路径：`ImageAssetPool.tsx:326`「用于报告」现**禁用**；成果导 `.pptx` 无按钮。
- 后端：`skillResultPptxUrl` 备；图片资产池数据结构（`listAllAssets`+`asset_type`）已就绪。
- 缺：报告配图闭环 + PPT 导出按钮。
- 补法：见外部研究"半自动 PPT 最小闭环"（先接 `skillResultPptxUrl` 是最快赢）。

**⑦ 三态补全（DoD 硬性）　🟠 中**
- `CockpitBoard` 加显式 loading；`SettingsOverlay:149-183` 批量读取失败要出 error 态。均低成本。
- **（本轮 B④/B⑤ 已纳入实现范围）**

**⑧ 斜杠命令入口　🟡 中低**
- `runCommand/listSkillCommands` 未接（`CommandPalette.tsx` 组件在但未调该 API）。补：营地输入框接斜杠命令预检路由（文本直跑/出图轻确认）。

**⑨ 死代码清扫　🟢 低（卫生项）**
- 删 7 个孤儿组件 + 5 个 mock 文件，一次抹掉全部残留 mock + 那个假按钮。**注意**：按"出包前 git 干净"铁律，这类删除要单独提交、归属清楚。**且清退紫黑 pages 已明确单独拍板，不在本轮。**

---

## SIGNAL SUMMARY

- **看板数据**：5 块看板 100% 真数据，零 mock 进看板。
- **未接线 API**：55/126（71 接线，0 裸 fetch 绕过）。
- **假按钮**：上线 UI 0，唯一真假按钮在死代码。
- **三态缺口**：3 处（CockpitBoard loading / SettingsOverlay error / KnowledgeBaseBoard empty 文案）。
- **hooks**：6/6 干净，零假数据兜底。
- **卫生项**：7 孤儿组件 + 5 mock 文件可清（须单独提交、归属清楚）。
