# 检查点① 专单 · 数据基地「建仓库 → 选文件/文件夹 → 一键清理」全链路

> 交付物③(与 bug 报告、总清单分开)。目标:你亲手在界面走完闭环。
> 依据:三动作只读盘点 + 验收映射核验(wwjvae3ya,4 agent + live :8000)。行号为盘点时锚点。**清单确认前不写码;N5 真切块 / N6 SSE 到步必停等你。**

---

## ⚠️ 开工前必答:3 个阻塞决策(先确认再动手 —— 盘点直接推翻了本单的两条前置假设)

**这些不定,ACTION1 无法落第一行。**

**D1 · 检查点0 的 DATA_ROOT 其实还没落。**
盘点核实:全后端**零 `DATA_ROOT`/`REPOS_ROOT` 定义**,只有 `config.DATA_DIR`(dev=backend/data,冻结=%LOCALAPPDATA%/ROM-AI,`ROMAI_DATA_DIR` 覆盖)。本单假设"检查点0 已统一 DATA_ROOT"**不成立**。
→ **拍板**:`REPOS_ROOT = DATA_DIR/'repos'`(默认;dev/冻结/override 自动继承),还是 DATA_ROOT 另立可覆盖根?**且检查点0 是否先于本单落地**(你此前定检查点0 置顶——那本单严格应排在它后面;是否要我把检查点0 先做完再动本单?)

**D2 · "建仓库" 与既有 `repository_root_path` 数据模型冲突。**
今天:`AppSetting.repository_root_path` = **单一**受管根,repo-mode 下**所有**项目都落 `{root}/{项目名}`([models.py:92](backend/app/models.py:92)、[uploads.py:36-62](backend/app/uploads.py:36))。本单要的是**多仓库**,每个 = `DATA_ROOT/repos/<slug>` + **独立 SQLite 仓库行**。两种模型不兼容。
→ **拍板**:(A) **repo 一等实体**(新 `repositories` 表 + project FK,对应你"落 SQLite 项目/仓库行"字面),还是 (B) **repo == project 1:1**(复用 projects 表,加 `repo_slug`/`rel_path` 列,不建新表)?**这决定 alembic 0023 加表还是加列、端点是 `POST /api/repos` 还是扩展 `POST /api/projects`**。并定旧 `repository_root_path` 去留(保留为全局 override / 弃用 / 并存)。

**D3 · WebView2 拖放拿不到绝对路径,与"禁 web input"红线冲突。**
浏览器/WebView2 的 `dataTransfer.files` 只给字节、**不给绝对路径**(只有 Electron 的 `File.path` 才有)。你要"拖放作第二入口"且"禁 web input"——WebView2 下二者互斥。
→ **拍板**:(a) 用 pywebview 原生 drop 事件拿路径,还是 (b) 拖放退化为字节入口(=破红线)?

> 另有 4 个次级决策(sha256 去重范围=全局 vs 本仓、staging 大文件哈希时机、_assets 落仓库根 vs 内部 uploads、worker 模型)——列在 §五。其中 **worker 模型已被你的验收"关闭重开续跑"强制定死为 DB-backed**(见 §四 D-worker)。

---

## 一、动作 1:建仓库(`POST /repos` → mkdir + _assets + SQLite,重名 exist_ok=False)

**现状**:`POST /api/projects` **只建 DB 行**、不建目录/不建 _assets/无 slug([projects.py:363-369](backend/app/routers/projects.py:363));全后端 **无任何 mkdir 端点**;唯一受管根 mkdir 助手 `uploads._unit_dir` 是 **exist_ok=True 懒建**([uploads.py:77-81](backend/app/uploads.py:77));`_assets` 硬编码 `UPLOADS_ROOT/{pid}/_assets` 懒建([project_files.py:366-369](backend/app/routers/project_files.py:366));**全后端 exist_ok=False 零命中**(无任何重名目录拒绝)。

| 项 | 类 | 文件 | 预估 | 说明 |
|---|---|---|---|---|
| repo safe_slug | 复用 | `backend/app/safe_paths.py` | +0~6 | `sanitize_filename` 已保留 CJK("襄阳"→"襄阳")、去非法/保留名、255 截断。**注:非拼音转写**——若要 ASCII 目录名才需新建,默认保留中文(NTFS/UTF-8 合法) |
| 目录逃逸守卫 | 复用 | `backend/app/safe_paths.py` | +0 | `validate_path(REPOS_ROOT, slug)` 挡白名单+symlink 逃逸([uploads.py:115](backend/app/uploads.py:115) 同款) |
| 端点脚手架 | 复用 | `backend/app/routers/projects.py`、`database.py` | +0 | 仿 create_project(add→commit→refresh,201)+ `Depends(get_db)` |
| **REPOS_ROOT 常量 + repos 布局** | **新建** | `backend/app/config.py`、`uploads.py` | +8~15 | **D1 阻塞**。定义 `REPOS_ROOT=DATA_DIR/'repos'` + 仿 `_reject_nested_with_uploads` 防嵌套。**其余全依赖它,先做** |
| create_repo_dir 助手(sanitize→validate→mkdir exist_ok=False→+_assets) | 改造 | `backend/app/uploads.py` | +25~40 | `_unit_dir` **旁加兄弟函数**(不改 `_unit_dir`,懒写仍靠 exist_ok=True)。这是**全后端唯一** exist_ok=False 重名报错点 |
| Repository 模型 + alembic 0023 | 新建 | `backend/app/models.py`、`0023_repositories.py` | +30/+35 | **仅 D2 选 (A) 时**。列 id/name/slug(uniq)/rel_path/project_id?/created_at;down_revision='0022_admin_passcode';照 [0017](backend/alembic/versions/0017_file_assets.py) 加守卫 |
| **POST /api/repos 端点**(dir+_assets+SQLite,重名→409) | 新建 | `backend/app/routers/projects.py`、`schemas.py` | +40~70/+15 | 动作1 端点。**顺序**:create_repo_dir **先**(重名 FileExistsError→409)→**再**写 DB 行→失败回滚 dir(**回滚顺序见 D2/§五,今无 exist_ok=False 无此保证**) |
| _assets_dir 仓库根感知(推迟) | 改造 | `backend/app/routers/project_files.py` | +0 now/~+8 later | 动作1 只**建** `{repo}/_assets`;写图归属动作3。仅标记,本动作不改(最小改动) |

---

## 二、动作 2:选文件/文件夹 + staging 收料单

**现状**:**原生对话框桥完全不存在**([launcher.py:211](desktop/launcher.py:211) `create_window` 无 js_api、全站零 `create_file_dialog`);今日主"选文件"入口正是红线要禁的 **web `<input webkitdirectory>` 字节上传**(KnowledgePage)+ CleanupWizard 手输框;**无任何 staging/收料单端点**;去重仅 name+size([project_files.py:106](backend/app/routers/project_files.py:106)),**无 sha256**(全后端 hashlib 只在 admin 口令)。

| 项 | 类 | 文件 | 预估 | 说明 |
|---|---|---|---|---|
| **pywebview 原生对话框桥**(js_api→create_file_dialog 返绝对路径) | 新建 | `desktop/launcher.py` | +45~75 | `create_window(js_api=Api())`;`pick_files()`→`OPEN_DIALOG(allow_multiple)`、`pick_folder()`→`FOLDER_DIALOG`→ `list[str]` 绝对路径(pathlib+UTF-8,禁 web input)。exe 专项验中文路径 |
| 前端原生选取桥 + 能力探测 + dev 降级 | 新建 | `frontend/src/lib/nativePick.ts`、`lib/api.ts` | +40~70 | 有 `window.pywebview.api`→原生;无(dev 浏览器)→降级 FolderPicker/手输。api 加 `staging(paths)` 方法 |
| FolderPicker 复用为 dev 降级入口 | 复用 | `frontend/src/components/FolderPicker.tsx` | +0~15 | 已走 `/api/filesystem/list-dir` 返真绝对路径、天然合规;至多加多选 |
| **POST /api/staging 收料单**(按夹分组+类型统计+supported+hash 标已在库) | 新建 | `backend/app/routers/staging.py`、`main.py`、`schemas.py` | +100~150 | 收 abs 路径→文件夹 rglob→按父目录分组;每文件 name/abs/rel/size/ext/`supported`(复用 [parsing.is_supported:70](backend/app/parsing.py:70))/sha256/已在库;可从 git a17b5fe 复活 batch-ingest 分组骨架。**只读预览,不写盘、不 move** |
| sha256 去重(升级 name+size→hash)+ ProjectFile.content_hash 列 + 迁移 | 改造 | `backend/app/models.py`、`project_files.py`、`inbox.py` | +45~80 | 加 `content_hash` 列 + alembic;staging 算的 sha256 **须透传给动作3**(断点续跑基石);大文件分块流式哈希 |
| 拖放第二入口→接 staging | 新建 | `frontend/src/seasky/components/data/CleanupWizard.tsx`、`KnowledgePage.tsx` | +30~60 | **D3 阻塞**:今 onDrop 读字节无 abs 路径;喂 abs-path staging 须 pywebview 原生 drop,否则退字节管线(破红线) |

---

## 三、动作 3:一键清理(任务队列 + SSE 五段 + 断点续跑 + 待重试区)—— 红线密集区

**现状**:**无任务队列、无 SSE**(grep 零 StreamingResponse/EventSource);五段散在 3 个互不相连端点(parse 在 upload_file、index 在 index_file、抽图 在 extract-assets)+ 扁平 inbox 循环;**"切块"只是页/片溯源**([retrieval.py:142-154](backend/app/retrieval.py:142) 一 doc 一行 FTS,`content_chunks_json` 是溯源 JSON 非检索 chunk);**无断点续跑、无待重试区**;抽图**无超时**(仅数量上限 80)。

| 项 | 类 | 文件 | 预估 | 说明 |
|---|---|---|---|---|
| **SSE 进度流端点** | 🔴新建 | `backend/app/routers/project_files.py`、`main.py`、`schemas.py` | +120~180 | **N6 · 红线先停**。`text/event-stream`,逐文件×逐段 yield `{file_id,filename,stage,status,...}` |
| **真·切块 + 逐块 FTS** | 🔴改造 | `backend/app/retrieval.py`、`parsing.py`、`models.py`、`project_files.py` | +180~260 | **N5 · 本轮做不降级 · 红线先停**。今 FTS 一 doc 一行;需真 chunker(按已有页/片边界+定长重叠)+ chunk 级 FTS(加 doc_id/file_id 列)。**幂等:按 file_id/hash 先删后插**,否则续跑产生重复 chunks |
| 任务队列/五段编排器 | 新建 | `backend/app/ingest_pipeline.py`(新)、`project_files.py`、`parsing.py`、`image_assets.py` | +220~320 | 全新模块,**复用**既有片段:识别=is_supported、抽取=parse_file、索引=index_one、归档=extract_assets。**工作口径红线:全新管线,写码前停确认** |
| 断点续跑(持久化逐文件/逐段进度 + hash 幂等) | 新建 | `backend/app/models.py`、`0023_ingest_progress.py`(新)、`ingest_pipeline.py`、`project_files.py` | +140~210 | **须新持久化**(见 §四):content_hash+ingest_stage+ingest_status+stage_error;必走 alembic 0023 |
| 待重试区(失败/超时可查询区 + 重试端点,非阻塞) | 新建 | `backend/app/models.py`、`project_files.py`、`ingest_pipeline.py`、`schemas.py` | +90~140 | 今超时件被直接跳过([project_files.py:140](backend/app/routers/project_files.py:140))、失败件 move 到磁盘 `_failed/` 不可查。需字段+GET 列表+POST 单文件/单段重试 |
| 术语"索引"(非"向量")校对 | 复用 | `backend/app/retrieval.py`、`knowledge.py` | +0~10 | 后端已全 FTS5/"索引",向量零命中;仅保证新 SSE 段标签/新字段用 识别/抽取/切块/索引/归档,不引入"向量" |
| 抽图→_assets 复用 + 大文件超时降级 + 布局对齐 | 改造 | `backend/app/routers/project_files.py`、`image_assets.py` | +50~90 | 归档段。复用 `_extract_and_store_assets`+FileAsset+make_thumb;**补**:抽图加时间预算(仿 OCR 22s/PARSE 30s)+ daemon-thread join 降级→待重试区;_assets 落点对齐(见 §五) |

---

## 四、断点续跑 / 待重试区 —— 核验裁定(逐列核过 models.py:199-234)

- **ProjectFile 无 hash 列、无逐段进度列**。现有:`parse_status`(单一抽取结论,非五段)、`status`(仅 active/trashed)、`indexed_doc_id`(仅"索引"一段幂等)、`content_chunks_json`(页/片溯源非检索 chunk)。
- **断点续跑须新增持久化**(现不存在):`content_hash + ingest_stage + ingest_status + stage_error(+updated_at)`。两形态皆走 **Alembic 0023**(照 [0018](backend/alembic/versions/0018_project_file_chunks.py) 守卫加列;`create_all` 只自举新库→迁移必需;down_revision='0022_admin_passcode'):(A) project_files 加列,或 (B) 独立 `ingest_progress` 表按 content_hash 建键。
- **待重试区不必新表**:可作进度行之上的视图(`ingest_status IN('failed','timeout')`)——但列不存在则视图不成立。
- **D-worker(被验收定死)**:你的"中途关闭重开→续跑不重复"**强制**进度**产生即提交 DB**(不能留在 SSE 生成器内存),故编排器必须 **DB-backed 后台**,不是自由二选一。

---

## 五、次级待拍板(§开工前 3 条之外)

1. **sha256"已在库"范围**:全局(任意项目命中)vs 仅本仓/项目?
2. **staging 大文件哈希时机**:选取期即算全量 sha256(190MB PDF 会卡)vs size+快速指纹先行、入库补全?
3. **_assets 落点**:落仓库根 `DATA_ROOT/repos/{slug}/_assets`(用户在自己文件夹看得到,闭环完整)vs 留内部 `UPLOADS_ROOT/{pid}/_assets`(今硬编码,[_serve_asset](backend/app/routers/project_files.py:500) 也假定在 uploads 根)?**落仓库根需改 _assets_dir + 服务/ validate_path**。
4. **exist_ok=False 回滚顺序**:dir-先-DB-后(重名先炸)vs DB-先-dir-后,失败清理另一半,不留孤儿。

---

## 六、验收映射(你的两层验收 → 责任项 + 缺口)

**[dev] 手输襄阳 → staging 列 105 文件+分类+36 张重复标灰 → 一键清理逐段进度 → 回执 chunks 数与库内一致**
- 覆盖:手输(nativePick dev 降级 + FolderPicker + CleanupWizard 手输框)→ staging 端点 → SSE 端点 → 真切块(chunk 计数来源)。
- 🔴 **最硬依赖=真切块**:无真切块则"chunks 数"无处可数(今 FTS 一 doc 一行、content_chunks_json 只是溯源)。
- ⚠️ **验收前置缺口(验证者补)**:"36 张重复标灰"预设 36/105 **跑前已在库**——须先做一次部分入库当 fixture,否则 duplicates=0、该行不可满足。
- ⚠️ **诚实性缺口**:切块跑在 `MAX_TEXT_CHARS=200000` 截断文本上([parsing.py:149](backend/app/parsing.py:149));190MB PDF 回执 chunk 数只反映截断头部却仍"与库一致"→回执须带 `truncated_at_page/total_pages`(列已存在)否则数值通过、语义虚假。
- ⚠️ **契约缺口**:"105"指全部还是仅 supported?须与一键清理实际处理集一致,否则总数/去重数对不上。

**[dev] 拖同批第二次 → 去重生效 → 中途关闭重开 → 续跑不重复**
- 覆盖:拖放接 staging + sha256 去重 + 断点续跑 + 待重试区。
- 🔴 D3 拖放绝对路径冲突(见上);D-worker 被此行定死为 DB-backed。

**[exe] create_file_dialog 选襄阳文件夹(中文路径)→ 全流程走通**
- 覆盖:pywebview 桥 + 全链路 pathlib/UTF-8 + sanitize 保留 CJK。exe 增量=对话框+真中文 abs 路径,故**依赖每一个 item**,非单靠桥。
- ✅ **中文路径 exe 风险已排查**:OCR 是**纯 Python RapidOCR/onnxruntime、无 shell、吃 bytes**([ocr.py:73](backend/app/ocr.py:73) 传 `pix.tobytes("png")` 非路径),parsing/image_assets 亦纯 Python 无 shell → **抽取/OCR/抽图段不会在中文路径炸**(禁 bash 中文路径红线满足)。
- ⚠️ _assets 落点断裂(§五-3):若落仓库根而抽图仍硬编码 uploads,用户在自己仓库看不到抽出图→"归档"段视觉闭环断。

---

## 七、红线先停清单(到步必停等你)

- 🔴 **N6 SSE 进度流**(§三)—— 本轮做,写码前停。
- 🔴 **N5 真·切块 + 逐块 FTS**(§三)—— 本轮做不降级,写码前停。
- ⚠️ 工作口径红线("一写新管线就停")下,另三处 NEW 面即便未点名,写码前仍应停确认:**ingest_pipeline.py 五段编排器**、**Alembic 0023 新持久化**、**staging.py 新端点**。

---

## 八、执行顺序建议

```
[先答] D1 DATA_ROOT/检查点0 顺序 · D2 repo 模型(表 vs 列)· D3 拖放路径
   │
检查点0(若未落:DATA_ROOT 统一 + alembic 接入)——本单前置
   │
动作1 建仓库: REPOS_ROOT 常量(先)→ create_repo_dir → [0023 表/列]→ POST /repos
   │
动作2 选文件: 原生桥(launcher)+ nativePick + staging 端点 + sha256 列/迁移 →(D3)拖放
   │
动作3 一键清理: 断点续跑持久化(0023)→ 编排器 →〔🔴停〕真切块 →〔🔴停〕SSE → 待重试区 → 抽图超时
   │
[dev 验收]手输/拖放闭环 →[exe 验收]对话框+中文路径
```

> **注意**:动作2 的 sha256 列 与 动作3 的断点续跑持久化 都要 alembic 0023——**宜合并为一个 0023 迁移**(content_hash + 进度字段一次加齐),避免两次加列。
