# 方案 · ROM-AI 全栈提升总路线（2026-07-08 · 待批）

> 依据:六路并行全栈实证审计(五板接线/前端基建/后端路由面/数据层/质量工程/mock残留),关键结论均带 file:line,存疑处已由主会话复核(两处纠偏见 §一末)。
> 与蓝本关系:**蓝本(docs/decisions/数据基地2.0蓝本.md)是数据基地主线,路权已定,本方案不重写不越阶**——本方案补的是蓝本之外的三件事:①板块诚实度收尾 ②工程与资产安全 ③有证据的技术债。所有动作待批,不抢"起 P0"令。

## 〇、总判断(一句话)

**接线已真,债在暗处。** 五大板块渲染路径里已无一处 mock 进屏(5 个 .mock.ts 全部只被零挂载的孤儿组件引用);真正的风险不在"数字假",而在四类暗处:**错误静默**(b3/b4 失败伪装成空态)、**承诺超前**(收件箱"60s自动扫描"实际永不扫描、甲方画像"点开即读"实际无 onClick)、**资产单点**(git 零远程,32 分支 24 笔领先全在一块盘上)、**成本黑箱**(MoA 花真钱但 token 靠字符数估算、不落库)。

## 一、审计地面真相(数字账,全部实证)

| 域 | 数字 |
|---|---|
| 后端 | 25 router / **125 端点**;17 表 / 22 迁移(止于 0022);**216 用例**/35 文件(不联网不花钱);22 技能全走 LLM(零纯规则);MoA 4 预设(1 已接 UI / **3 个 API-only 暗能力**);llm.py 仅 chat、DeepSeek 单 provider、13 个调用点;生图走 APImart |
| 前端 | 5 板 1245 行**全接真源**;api.ts **119 方法**(seasky 活跃 50 / 仅退役紫黑引用 44 / 零引用 25 → **58% 面积无活跃消费者**);9 hooks;romai:* 事件 4 个(2 闭环 / 1 死派发 / 1 死订阅);**7 个孤儿组件 + 5 个 mock 文件 ≈500 行死链**;紫黑 pages/ 17 文件冻结(tag retire-purple-final-20260706) |
| 质量 | 前端测试 **7 用例 vs 后端 216**(仅 CleanupWizard);无 CI;**git 零远程**;polish/three-states 领先 main **24 笔**(main 零独有,可纯快进);工作区 25 条未跟踪文档(含 2 份"待批"方案);`desktop/.env.bundle` **在场**(打包默认带真 key) |
| 蓝本增量核验 | content_hash/storage_mode/retry_at/extract_truncated 列、outlines/version_groups/version_members/archive_actions/health_reports 五表、/reveal、@语法、Ctrl+K —— **grep 全零命中,白纸一张**(无残骸,直接按蓝本建) |

**审计纠偏(主会话复核后推翻/修正的审计员结论,留痕):**
1. 审计称"routers/staging.py 与 routers/ingest.py 未跟踪(悬置改动)"——**错**,`git ls-files` 三文件全在跟踪中(会话开局快照过时)。当前悬置的 25 条全是文档/原型,零代码文件。
2. 审计称"ingest 五段"与代码 STAGES 4 段矛盾——核实 `app/ingest.py:38` 确为 4 段标签(落盘/解析/入库索引/归档抽图),前端与注释口径"五段"是文案漂移(小,归 T4)。

## 二、六条提升线

### T0 · 资产安全(独立于功能,建议最先批)
| # | 事项 | 证据 | 动作 |
|---|---|---|---|
| T0-1 | **git 建远程并推送** | `git remote -v` 空;32 本地分支/24 笔领先/6 份决议/全部复盘只存在这一块盘 | 你定远程目标(私有 GitHub/Gitee/内网/另一块盘 bare repo 均可),我配 remote+push 全分支。**这是认知资产可移植性铁律当前最大的违反状态:一次磁盘故障=全部认知资产归零** |
| T0-2 | **打包 key 防呆** | `desktop/.env.bundle` 在场(262B);romai.spec:44-46 只要它在就打进包 | spec 改为**默认排除**,显式设 `ROMAI_BUNDLE_KEY=1` 才打入;出包铁律加第 4 条"keyless 包出包前验证包内无 .env" |
| T0-3 | **25 条悬置文档定归属** | git status ?? 25 条:顶层 9 份分析md/plan.md/交接md/prototypes//输出-00/ + docs/ 下 8 份(含两份"-待批"方案) | 三分法过目:入库(至少两份待批方案+专单/清单类)/移 .tmp_run_logs(一次性)/删。出包铁律第 1 条要求工作区干净,这 25 条堵着 |

### T1 · 三态诚实补全包(前端小改,建议与"封板"同包——同属数据可信叙事收尾)
| # | 事项 | 证据(file:line) | 动作 |
|---|---|---|---|
| T1-1 | b3 错误态零渲染 | useHubLive.ts:40-49 收集 errs,HubBoard 全程不显示;四源任一失败=静默空列表 | HubBoard 加失败行(照 b0 live.err 模式) |
| T1-2 | b4 数据无 err 字段 | useCockpitLive.ts:16-28 无 err;:101-104 allSettled 失败静默丢弃→空态文案顶上 | CockpitLive 加 err + 板内显示;错误≠空库 |
| T1-3 | b0 无项目 loading 永挂 | useProjectLive.ts:26 projectId==null 直接 return,初始 loading:true 永不落地 | null 时置 loading:false,数字区显"—" |
| T1-4 | b0 部分失败静默 | useProjectLive.ts:47-50 仅 4 源全败才报 err | 改为逐源记错(照 useHubLive 模式)+板内提示 |
| T1-5 | **b3 甲方画像假按钮**(全 app 唯一) | HubBoard.tsx:118-125 button 无 onClick;:127-129 文案承诺"点开即读";getClientPortrait 服务挂出零调用 | 接线(点开 Popover 显画像,后端端点已有)——违"不假按钮"铁律,应修不应摘 |
| T1-6 | **b2 模型选择器装饰性** | constants.ts:47 三个虚构型号;AgentCampBoard.tsx:122,148 runSkill 的 model 参数恒传 '' | **待拍板二选一**:(a)映射到真模型名传后端;(b)撤控件。涉及"ROM Max/Pro/Lite"产品命名与 DeepSeek 单 provider 的映射关系,不能顺手定 |
| T1-7 | 小空态四处 | SkillLibraryPanel.tsx:51 空技能无文案;HubBoard.tsx:133-146 智能体列空白;KnowledgeBaseBoard.tsx:48 `typeStats.length \|\| '—'` 把真 0 显成'—';MeetingTile.tsx:156 空态句式不合规 | 统一按"暂无X。下一步指引。"句式补齐 |
| T1-8 | 死事件+注释漂移 | CleanupWizard.tsx:364 派发 recent-refresh 全仓零监听;CleanupOverlay.tsx:58 死订阅;CleanupFlowCard.tsx:5+SettingsOverlay.tsx:95 注释仍指认 CleanupOverlay 为权威面 | 删死派发或接真(打开"最近入库"浮层);注释改指 CleanupWizard |

### T2 · 事件断链修复(事件命名约定的直接执行,小而真)
| # | 断链 | 证据 | 动作 |
|---|---|---|---|
| T2-1 | **入库建新项目→项目切换器看不见** | ingest.py:103 会 _find_or_create_project;CleanupWizard 只派 knowledge-updated;projectBridge.ts:35-55 挂载拉一次、不监听任何事件、reload() 零调用点 | 新增 `romai:projects-updated`:入库完成(检测到新建项目)派发,projectBridge 监听重拉。**这是用户可感知的真 bug:入库完项目列表不出现新项目,直到重启** |
| T2-2 | 驾驶舱发通知→协作板横幅不更新 | useCockpitLive.ts:137-140 只 bump 自身;useHubLive 一次性闸 | 新增 `romai:broadcast-updated`,useHubLive 监听 |

### T3 · 死代码清退(拍板制,一次清干净)
- **清退对象**(全部零挂载实证):5 个 .mock.ts(含最具误导性的 PROJECTS_SEED 假项目/USAGE 假 KPI/罐头回复)+ 7 个孤儿组件(UsageDonut/WorkloadPanel/ProjectCalendar/BroadcastPanel/DataTypeTabs/KnowledgeCard/CleanupOverlay)≈500 行——**这就是 backlog#27 的答案:现役已全真,mock 只剩未删的孤儿**。
- **紫黑 pages/ 17 文件**(642行 CampPage 等,58% api.ts 面积的唯一消费者):维持冻结(tag 可回滚)或清退,**待拍板**——若清退,api.ts 可同步瘦身 69 个方法(44 紫黑独占+25 零引用,其中零引用里 7 个对应后端备用端点,保留注释标注即可)。
- 后端 7 个前端零调端点(reparse/export.md/ingest快照等):**不删**,多为合理备用;登记注释"预留"。

### T4 · 后端债清账(有证据的半成品)
| # | 债 | 证据 | 动作 |
|---|---|---|---|
| T4-1 | **收件箱"自动扫描"从未存在** | SettingsOverlay 占位文案承诺"60s 自动扫描";inbox.py 为扫描制,唯一触发按钮在**已退役**紫黑页(KnowledgePage.tsx:687);seasky 只显示 pending 计数;后端 lifespan 无调度器 | **待拍板二选一**:(a)后端加 60s 轮询线程(inbox.py 注释已说明弃 watchdog 的原因,轮询线程可行);(b)文案回撤+seasky 加"立即扫描"按钮。现状=配了收件箱文件永远堆着,pill 数字涨但никогда入库 |
| T4-2 | ingest 进度内存态 | app/ingest.py:33 _JOBS 内存,重启即丢;注释明确"依赖 0023 迁移" | 随蓝本 P1(ingest 三件套本就要开 0023 迁移)一并做 ingest_jobs 表,不单独动 |
| T4-3 | **MoA 成本黑箱** | moa.py:478-490 字符数估 token;:496 DAILY_BUDGET_YUAN 声明无实现;:6 宣称的 MoAChain 表不存在(models grep 零);会诊成本不落库(review_checklist.py:123);驾驶舱只数次数无费用(boss.py:138-169) | 出一页方案待批:最小 ai_usage 表(每次 LLM 调用落 tokens_est/cost_est/task/model),驾驶舱 AI 用量卡从"数次数"升级"显费用";日预算累计才有依托。**花真钱的功能,账本不能是估算+不落库** |
| T4-4 | MoA 3 预设暗能力 | concept/compare/compete_moa 预设在(moa.py:383-430)、API 可达,UI 无入口(前端仅 review 发 mode=moa) | 随营地包(蓝本 C1 provider 卡片化时)给三预设开关或明档为"API-only" |
| T4-5 | 文案/注释漂移三处 | "功能/甲方/成本三专家"vs 实际设计版三视角(review_checklist.py:3+api.ts:656);"五段"vs STAGES 4 段;review history 端点 json.loads 无保护(:207) | 顺手修,不单独立项 |

### T5 · 主线与测试随行(引用蓝本,不重写)
- **主线不变**:封板(D2 库状态视图+闭环封板说明)→ P0 找得到 → P1…P5,等你"起 P0"令。
- **测试随行原则**:P0 检索属心脏——@语法解析、结果卡、reveal 各配组件/接口测试(照 CleanupWizard 7 用例的模式);hooks 事件驱动(useKnowledgeLive/projectBridge 监听)补测。**不为覆盖率而测,只守心脏链路和事件断链**——前端 7 vs 后端 216 的悬殊主要靠 P0-P1 实现期自然拉平。
- 蓝本已吸收的 Linkly 借鉴(D2 归封板/C1+C2 归营地包/D1 次级页)不再重复列。

## 三、排序建议(不抢路权)

```
即刻(独立,不碰功能): T0-1 git远程 → T0-2 key防呆 → T0-3 悬置归属
封板包(数据可信收尾,一包): D2库状态视图(蓝本) + T1全部 + T2两事件 + T4-5顺手修
拍板后独立小包: T3死代码清退 / T4-1收件箱 / T1-6模型选择器 / T4-3成本方案
主线: 蓝本 P0→P1(带0023含T4-2)→P2→P3→P4→P5,叫到才做
```

## 四、明确不做
- 不重构/不删紫黑 pages(冻结策略在案,清退单独拍板);不加新板;不碰锁版视觉面。
- 不做公网任何形态(MCP 隧道/远程访问,永久禁止项)。
- 不越阶实现蓝本 P2+;不为覆盖率写测试;不引入 CI 大件(单人本地流,远程建立后再议轻量 CI)。
- 不在本方案里重复蓝本已排期的功能(大纲/向量/版本链/归档审计/MCP)。

## 五、待拍板清单(批哪条动哪条)
1. **T0-1 远程目标选哪**:私有 GitHub / Gitee / 内网 git / 本机第二块盘 bare repo?
2. T0-3 悬置 25 条:按我给的三分法清单过目后执行?
3. 封板包范围:D2+T1+T2+T4-5 一包做,还是拆?
4. T1-6 模型选择器:接真映射还是撤控件?
5. T4-1 收件箱:后端 60s 轮询实现,还是文案回撤+手动按钮?
6. T3 紫黑 pages 17 文件:继续冻结还是清退(连带 api.ts 瘦身 69 方法)?
7. T4-3 MoA 成本落库:要不要出一页方案?
