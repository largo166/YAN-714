# ROM-AI EXE 启动动画设计方案

> **设计日期**：2026-06-28
> **设计师**：Orchestrator Agent
> **性质**：启动画面（Splash Screen）视觉设计与技术实现方案

---

## 一、小样文件

**文件路径**：`C:\ROM-AI-Claude 开发\splash_preview.html`

**打开方式**：直接用浏览器打开（双击文件），即可观看完整动画。

**动画时长**：约 8 秒（含退场），按 F5 可重播。

---

## 二、设计理念

### 2.1 核心概念：「蓝图 → 建筑 → 智能」

ROM-AI 是面向建筑设计前期的 AI 工作台，启动动画的叙事逻辑：

```
0.0s   黑屏（沉静）
0.3s   建筑网格浮现（蓝图感）
1.0s   几何线框展开（从二维到三维的空间感）
2.0s   金色粒子汇聚（能量注入）
2.5s   "ROM-AI" 逐字揭示（品牌确立）
3.0s   副标题淡入（定位说明）
3.5s   进度条启动（系统加载叙事）
6.2s   "READY" 闪烁（准备就绪）
7.0s   整体淡出（过渡到主界面）
```

### 2.2 配色系统

| 角色 | 色值 | 用途 |
|------|------|------|
| 深黑背景 | `#0a0a0c` | 主背景，科技沉稳 |
| 暗面 | `#111318` | 次级层次，建筑混凝土感 |
| 金色主色 | `#c9a96e` | 品牌色，建筑感暖金（呼应 ROM-AI 现有设计系统）|
| 金色高光 | `#e8d5a3` | 文字光晕、焦点强调 |
| 金色暗面 | `#8a7652` | 辅助线、边框 |
| 主文字 | `#e8e6e1` | 高可读性灰白 |
| 次级文字 | `#6b6860` | 副标题、说明 |

> **为什么是金色？** ROM-AI 现有的设计系统已使用金/棕色系（如 `exporters.py` 中的 `#b07a35` 边框色、 `#7a5a2a` 标题色）。金色既有建筑材料的质感（黄铜、石材），又在深色背景上形成优雅对比，区别于普通的蓝色科技风。

### 2.3 字体策略

- **品牌文字**：`font-weight: 200`（极细），`letter-spacing: 0.15em`，营造高级感
- **副标题**：全大写 + 宽字间距（0.35em），致敬瑞士平面设计的理性秩序
- **进度文字**：等宽字体（monospace），模拟终端/工程界面
- **系统字体栈**：优先使用系统自带的现代字体，不依赖外部字体加载（启动画面必须秒开）

---

## 三、动效拆解（6 层）

### Layer 1：蓝图网格（Blueprint Grid）

- 60px 间距的淡金色网格线
- 径向遮罩：中心清晰，边缘渐隐
- 出现方式：透明度从 0 到 1，1.2s 淡入
- **隐喻**：建筑设计的原点——蓝图/场地分析图

### Layer 2：几何线框（Geometry Frame）

- 外层正方形：从中心点向四周"绘制"展开
- 内层正方形：旋转 45° 后浮现，形成建筑轴测感
- 四角装饰：L 形角标依次出现，强化建筑图纸的标注感
- **隐喻**：从平面到立体的空间建构过程

### Layer 3：粒子系统（Particle System）

- 5 个金色粒子从几何中心向上飘浮
- 先出现、再放大、再消散
- 粒子轨迹模拟建筑节点连接
- **隐喻**：数据/能量的汇聚与流动

### Layer 4：品牌文字（Typography Reveal）

- "ROM-AI" 6 个字符逐个出现
- 每个字符：从下方向上滑入 + 模糊到清晰 + 微光脉冲
- 字母间隔：0.1s 错开，形成机械打字机般的节奏感
- 字间距：`letter-spacing: 0.15em`，留白呼吸感
- **隐喻**：AI 的"思考"过程——不是瞬间弹出，而是逐字建构

### Layer 5：进度叙事（Progress Narrative）

- 细线型进度条（1px 高度），金色渐变
- 进度条末端有发光头（模拟激光扫描仪）
- 非匀速填充：前 60% 快，后 40% 慢（模拟真实加载体验）
- 百分比数字实时更新（monospace 字体）
- 文字："LOADING SYSTEM..."，致敬工程界面
- **隐喻**：系统初始化不是瞬间完成，而是有真实感的进程

### Layer 6：状态切换（State Transition）

- 加载完成后，"READY" 文字闪烁出现
- 整体画面在 0.8s 内：轻微放大 + 模糊 + 淡出
- 退场动画完成后，由 PyWebview 切换到主界面
- **隐喻**：从"启动"到"就绪"的仪式感

---

## 四、技术实现方案（集成到 ROM-AI）

### 方案 A：前端 HTML 内嵌（推荐，0 依赖）

将 `splash_preview.html` 的内容直接作为字符串嵌入到 PyWebview 的启动代码中：

```python
# desktop/launcher.py 或启动脚本中
import webview

SPLASH_HTML = """<!DOCTYPE html>..."""  # 内联完整的 HTML

def show_splash():
    """显示启动画面，8秒后自动关闭并打开主窗口。"""
    splash = webview.create_window(
        'ROM-AI',
        html=SPLASH_HTML,
        width=900,
        height=600,
        frameless=True,      # 无边框
        on_top=True,         # 置顶
        resizable=False,     # 不可调整
    )
    
    # 8秒后关闭启动画面，打开主窗口
    webview.start(
        func=lambda: _delayed_launch(splash),
        gui='edgechromium',  # Windows 用 Edge 内核
    )

def _delayed_launch(splash_window):
    import time
    time.sleep(8)
    splash_window.destroy()
    # 打开主窗口
    main = webview.create_window(
        'ROM-AI · 建筑前期智能工作台',
        'http://127.0.0.1:5173',  # 前端 dev server 或打包后的文件
        width=1400,
        height=900,
        min_size=(1200, 700),
    )
```

**优点**：零额外依赖，HTML/CSS 内联无外部资源，启动秒开。

### 方案 B：独立窗口 + 进程间通信

```python
# 启动流程
1. 先启动 FastAPI 后端（127.0.0.1:8000）
2. 显示 Splash 窗口（webview，无框架，置顶）
3. 在后台检查后端 health 端点
4. 后端 Ready 后，Splash 显示 "READY" 并淡出
5. 关闭 Splash，打开主窗口（加载前端）
```

这种方式更真实——进度条对应的是后端实际加载进度，而非固定动画。

### 方案 C：GIF / MP4 视频（不推荐）

如果用视频：
- 需要 ffmpeg 生成，增加打包体积
- 需要视频解码器，增加兼容性风险
- 无法与加载状态同步
- 文件大小远大于 HTML（视频 1-5MB vs HTML 12KB）

**结论：HTML/CSS 动画是最佳方案。**

---

## 五、变体方案（备选）

### 变体 A：更冷峻的「参数化」风格

把金色改为青色（`#4ecdc4`）+ 黑色，几何元素增加参数化曲线（Bézier 流动线条），模拟 Grasshopper/Rhino 的算法设计美学。

适合：更偏向数字化/参数化设计的事务所。

### 变体 B：更温暖的「材料」风格

增加混凝土纹理、纸张质感、手绘线稿感，品牌文字用衬线字体（如思源宋体），营造手工模型感。

适合：注重材料性和手工感的精品事务所。

### 变体 C：更极简的「苹果」风格

纯黑背景 + 单一白色文字 "ROM-AI" + 一个微妙的光晕，没有任何装饰元素，只有 3 秒的淡入淡出。

适合：追求极简的高级用户。

---

## 六、与现有 ROM-AI 设计系统的对齐

| ROM-AI 现有元素 | 启动动画中的对应 |
|-----------------|-----------------|
| `exporters.py` 中的 `#b07a35` 边框 | 金色主色 `#c9a96e`（同一色系，更明亮） |
| `exporters.py` 中的 `#7a5a2a` 标题 | 金色暗面 `#8a7652` |
| 前端 Tailwind + shadcn 风格 | 同样基于 CSS 变量，便于日后统一主题切换 |
| 黑色/深色主题 | 深黑背景 `#0a0a0c` 完全对齐 |
| 建筑/设计行业属性 | 蓝图网格 + 几何线框 + 建筑节点粒子 |

---

## 七、给 Claude 的 Prompt（如需让他生成）

```text
为 ROM-AI 桌面应用生成一个 EXE 启动画面（Splash Screen）。

技术约束：
- 用纯 HTML/CSS 实现，所有样式内联，不依赖外部文件
- 目标是内嵌到 PyWebview 中作为 frameless 窗口
- 动画总时长 6-8 秒，结束后可切换到主窗口

视觉要求：
- 深色背景（#0a0a0c），科技感但不冰冷
- 建筑行业属性：蓝图网格、几何线框、建筑节点
- 品牌色：金色/琥珀色系（#c9a96e）
- 品牌文字 "ROM-AI" 用极细字重（200）+ 宽字间距
- 副标题 "建筑前期智能工作台"
- 有加载进度条（非匀速，前快后慢）
- 有 "READY" 状态提示
- 退场时整体淡出+轻微模糊

动效顺序：
1. 建筑网格淡入（蓝图感）
2. 几何线框从中心展开（建筑线框）
3. 金色粒子向上飘浮（能量汇聚）
4. "ROM-AI" 逐字揭示（品牌确立）
5. 副标题淡入
6. 进度条启动
7. 完成提示 + 整体退场

不要引入任何 JavaScript 库，只用原生 CSS 动画和少量 JS 计时器。
```

---

## 八、验收清单

- [ ] 打开 `splash_preview.html` 能完整播放动画（F5 可重播）
- [ ] 8 秒内所有动画元素不卡顿、不错位
- [ ] 无外部依赖（断网也能打开）
- [ ] 字体在各系统上正常渲染（Windows/macOS/Linux）
- [ ] 响应式：窗口大小改变时，内容始终居中
- [ ] 可集成到 PyWebview 的 `frameless=True` 模式
- [ ] 可扩展：进度条能与后端 health 检查联动（可选）

---

> **附注**：当前小样是纯视觉动画。如需与后端加载状态联动（进度条显示真实的后端启动进度），需要增加 WebSocket 或轮询 `http://127.0.0.1:8000/health` 的逻辑。建议第一阶段先上线固定动画，第二阶段再升级为"真实进度"。
