# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
backend/.venv/
backend/data/
backend/data/uploads/
*.db
*.sqlite3

# ── Node / 前端 ──
node_modules/
frontend/dist/
dist/
*.tsbuildinfo

# ── 环境 / 密钥（.env.example 仍纳入版本控制）──
.env
.env.*
!.env.example
!desktop/.env.bundle.example
*.local

# ── 日志 / 临时 ──
logs/
*.log

# ── 运行产物 / 临时工作目录(2026-07-07:CDP验收脚本·截图·解包临时目录·可分发链接,一律不入库) ──
.tmp_run_logs/
_tmp_*/
*.lnk
# 内测分发包(exe+文档 zip,体积大/含可分发产物,不入库;归档在本地 release-internal/)
release-internal/

# ── OS / IDE ──
.DS_Store
Thumbs.db
.idea/
.vscode/

# PyInstaller 桌面打包产物(不入库)
build_exe/
dist_exe/
build/
*.spec.bak
# 放在仓库根的可分发 exe(预置真 key,绝不入库;含 ROM-AI-v1.exe 等版本号产物)
/ROM-AI*.exe

# 发布产物目录(件1 T0-3,2026-07-08):归档在 C:\ROM-AI-发布归档\,不进仓库树
输出-00/

# 本地会议转写模型目录(1.6GB,首次下载,永不入库)
.romai_data/
